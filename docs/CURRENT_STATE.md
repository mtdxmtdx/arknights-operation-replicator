# 当前状态

更新时间：2026-08-10 12:30:22 +08:00

## 当前目标

作业编辑器已有独立使用说明，覆盖人工打轴流程、动作编辑、保存运行，以及当前 MAA Tile Pos 索引中
主线、常驻、活动、危机合约、集成战略、生息演算、保全派驻和特殊玩法的 `stageId` 格式与查询方法。
编辑页实时尺子和部署后 Resume 稳定修复继续保留；相关 GUI 与 HS-9 现场仍由用户实机确认。

## 现役实现与已验证成果

- AFA 负责自动开局暂停、普通暂停/恢复、`33ms` 逐帧、暂停技能和暂停撤退；Rust 负责部署拖拽、
  鼠标定位/停靠和单次 AFA 热键触发。`33ms` 已是预检必需项，失败时不回退旧 Rust 时序。
- 部署方向手势先保留 MAA 的有限整段平移，再将剩余越界端点限制到 Windows 客户区；2560×1440
  下 1-7 `(7, 2)` 向上部署不再产生负坐标。修复由独立本地提交 `d8ad54b` 跟踪。
- 第 0 帧“开始复刻”执行两次人工焦点交接、编队绑定和新尺子样本接管；Runner 不发送开局暂停，
  也不自动抢游戏焦点。动作前持续复核尺子，动作后只接受新水位上的目标帧可信暂停样本。
- HS-9 日志确认 F752/F991/F1586 的 Deploy 均先通过动作确认，但随后的单次 `ReleasePause` 分别
  收到 119/121/121 条可信 `1x_paused` 后超时。非最终 Deploy 现增加 500ms 同目标帧暂停稳定窗口；
  窗口内持续消费尺子样本，异常立即中止，不盲目重发 Resume。专用回归覆盖稳定等待与准确错误原因。
- Pulse 收尾、运行期 Pause 帧宽限和 Resume running 回执均有状态机回归覆盖；同帧动作分别使用
  新样本水位，最终 Finish 为零输入。
- `afa-step-test.exe` 与主复刻使用同一个 AFA `33ms` 热键，最多派发 500 次并由尺子稳定结算每次
  0/1/≥2 帧结果；缺少热键会让主程序预检失败。旧的“首动作至少第 60 帧”误判也已从规则、
  验收文档、示例作业和编辑器诊断中移除，尺子越过目标时的安全中止仍保留。
- 2026-07-29，用户明确确认当前验收二进制的 M8 §3–§4 全部通过：开始门禁、手动焦点交接、
  已有档案恢复、paused 人工绑定、running 无档案安全中止、Deploy/Skill/Retreat 全流程、热键无
  重复派发、零跨帧以及最终目标帧 `1x_paused` 零输入收尾。权威记录见
  [当前版本实机验收](REAL_MACHINE_ACCEPTANCE.md)；截图和运行日志未纳入 Git。
- 2026-07-30，用户明确确认 AFA 逐帧构建 `09EEED203464B64D55F5FA53803006B7BB827EAF35648888E42D58A0D064AB6D`
  按验收手册完成 M9 录像与 CSV 10/10；录像、CSV 和运行日志由用户保留，未纳入 Git。
- 非零可信暂停帧可冻结“继续截止帧”；前缀动作标为“用户确认完成”，新会话按作业播种场上状态，
  最终接管不得越过下一动作帧。跨编队头像档案支持后续再部署；继续结果不能计入 M9。
- 人工作业编辑器提供保留未知 JSON 字段的 `JobDocument`、严格 `Copilot` 编译边界、分轨时间轴、
  同帧排序、撤销/重做、原子保存、编队维护和逻辑地图选点。编辑模式不探测 AFA、不创建
  `Session` / `Runner`、不定位游戏窗口且不产生输入。
- `docs/JOB_EDITOR_GUIDE.md` 记录完整编辑流程，并按本机 MAA Tile Pos 的现有条目归纳全部地图家族；
  文档明确推荐唯一 `stageId`，说明 `code` 重名、`#f#` 变体、肉鸽内部格式和特殊模式帧源限制。
- 编辑页在连接尺子后独立显示最新累计绝对帧；轻量定时器每 33ms 只读取共享快照并更新一个 UI
  属性。400ms 状态定时器继续承担 AFA、窗口等较重探测。“跟随尺子”只决定时间轴光标是否同步，
  不再决定实时绝对帧是否可见。
- 保存/另存为后的 GUI 闪退已由回归测试覆盖：保存回调在刷新 Slint 前释放 `RefCell` 可变借用。
- 动作属性面板的输入控件在动作选择变化时会显式同步当前动作值，避免首次编辑后继续显示上一动作
  的内容；回归测试锁定帧数、X/Y 和说明的重新绑定调用点。
- 会写入作业历史的标题、关卡、帧数、X/Y 和说明使用 `CommitLineEdit`：键入期间只保留控件草稿，
  回车或失焦才提交一次。`249` 不再形成 `2 → 24 → 249` 三条撤销记录；确认后一次撤销回到旧值。
- 动作干员改为带占位项的编队下拉菜单，只能选择提前维护的编队成员；部署方向提供上、下、左、右、
  无方向五项中文下拉，并继续写入兼容的内部 `Direction` 枚举。注释动作不显示干员下拉。
- 编队干员添加框按名称片段搜索本地 MAA `battle_data.json`，前缀结果优先、去重并排除已在编队的
  名称；点击候选即加入。目录不可用时仍允许手动输入，且该路径不初始化 OCR、不截图、不访问 AFA 或
  游戏窗口。
- “+ 技能 / 装置”动作同时支持名称和逻辑格子：显式 location 优先，location-only Skill 合法并进入
  “地图装置”轨；普通干员技能可清除格子，恢复按 Session 部署记录定位。预置装置不扫描部署栏、
  不弹延迟绑定且不写头像档案。
- 编队识别以部署栏公共旗标作为卡片存在证据；职业模板匹配失败时卡片仍进入人工绑定面板，职业显示
  “未知”。回归测试覆盖“旗标命中但职业未知”不能漏卡，避免只有先锋模板命中的编队无法绑定。
- `opers` 现只表示开局编队；Deploy 可首次引入召唤物/装置，后续 Skill/Retreat 可引用。编辑器以
  `frame_replicator.deferred_targets` 保存独立目标名单，并与开局编队共同提供动作目标下拉。
- Session 启动时从 `BindingStore.avatars` 恢复所有 Deploy 目标的全局头像。目标帧无法唯一匹配时
  弹出“新增召唤物绑定”；历史头像多卡歧义保留候选供人工确认，不自动猜测。
- 延迟绑定期间持续只读复核尺子，不调用 AFA、触控或逐帧。取消、running、非 1x、离战或帧号变化
  立即安全中止；绑定后必须切回游戏并取得新的同目标帧可信 `1x_paused` 样本才允许部署。
- 启动绑定只展示未被 Session/全局/编队头像解释的可见卡片，下拉选项来自作业 `opers` 中尚未绑定
  的名称。不可见名称本身不再触发面板；确认后由工作线程先校验并保存，再关闭面板并提示点击游戏。
- 复刻页“扫描编队”读取 MAA 姓名锚点、PaddleOCR ONNX 模型/字典、`battle_data.json` 和任务参数；
  结果逐槽显示头像、OCR、置信度与来源，只有作业精确匹配默认选中，用户可修正或忽略。
- 确认结果只形成一次性 Pending，绑定作业编队指纹、游戏 HWND、尺寸和 30 分钟 TTL；只供下一次
  FromZero 使用，“继续”、重新装载作业、取消、窗口变化和过期均不复用。
- F0 恢复顺序为 Session/global/profile 优先，再对未识别卡片做职业约束、多尺度 NCC。必须行列
  互为唯一最佳、超过 MAA 阈值且与次优项相差至少 0.05 才写入头像/profile，否则回退人工绑定。
- `bindings.json` 与 `config.json` 已统一使用同目录临时文件、同步落盘和 write-through 原子替换；
  扫描或确认本身不写绑定，只有 F0 桥接/人工绑定事务成功后落库。
- 复刻页日志已改为带标题和操作提示的独立滚动控制台；文本首选高度显式驱动 viewport，正文区域
  直接处理滚轮并保留右侧滚动条拖动区。Slint 编译检查已通过，GUI 手感待用户实机确认。
- 当前自动门禁通过：`cargo test --workspace` 共 259 项，包含编辑页实时尺子帧、部署后稳定屏障、
  Resume 专用超时原因、地图装置 Skill/location、MAA 干员目录搜索、真实 MAA OCR 资源/ORT 初始化
  和部署四方向客户区边界回归；
  `cargo clippy --workspace --all-targets -- -D warnings`、
  `cargo build --release`、`cargo fmt --all -- --check` 和 `git diff --check`。
- 许可证审计已区分项目自身、移植上游、Rust 运行依赖和系统组件：整体仍为 AGPL-3.0-only；
  `THIRD-PARTY-LICENSES.md` 从锁定的 Windows x64 主程序运行图列出 342 个 crate+版本记录；Slint
  选择 GPL-3.0-only，静态 ONNX Runtime 1.22.0 保留 MIT 义务，DirectML/VC Runtime 作为系统组件
  不进入当前发行包。
- 长期边界与时序决定位于 `docs/decisions/0001`–`0013`；领域术语以 `CONTEXT.md` 为准。

## 版本与发布状态

- 分支：`develop`；作业编辑器完整说明及 README 入口属于当前版本。用户本地
  `examples/hs9.json` 继续保持未跟踪且未修改。
- M8 验收基线二进制：大小 `14,710,272` 字节，SHA-256
  `4D510B3146FA40C95437E3E2914E7E8E42A2C91C53CFA8A8E035EEFF267AF8BD`。
- 当前本地构建：`target\release\repl-app.exe`，大小 34,479,104 字节，SHA-256
  `9C642D0046C87C7567E3F5C12037B6833A900AD55239ED3F57D0BB203415B54A`；体积主要来自静态链接
  ONNX Runtime，发布时无需额外 DLL。它包含 M8 后的编辑器、继续、AFA `33ms`、延迟绑定、启动
  绑定修复和战前识别改动，但尚未完成这些新增界面的实机验收，不能反向替换 M8/M9 验收记录。
  `initial_gap_ms` 只为旧配置兼容保留，不再影响主复刻。
- 状态：地图装置技能已 pushed、实机待验；目录搜索、部署后稳定屏障和编辑页实时帧已 locally
  verified、未 pushed、实机待验；此前战前 OCR/F0 桥接已 pushed。既有 M8/M9 仍为 real-machine
  verified；本项目没有独立 PR、部署或线上服务环节。

## 未完成 / 待验证

- 风险确认式“继续”：按[当前版本实机验收 §5](REAL_MACHINE_ACCEPTANCE.md)核对 F11、F1、冻结边界、
  最后动作后禁用和缺头像档案门禁。自动测试不能证明用户确认的前缀动作与真实画面一致。
- 编辑器 GUI：按[编辑器实机验收](EDITOR_ACCEPTANCE.md)核对文件对话框、地图点选、长文本输入、
  编队目录搜索、回车/失焦提交、撤销粒度、干员/方向下拉（含无方向）、动作切换属性隔离、窗口缩放、
  33ms 实时尺子帧和严格零输入边界。
- 地图装置技能：按[编辑器实机验收 E4](EDITOR_ACCEPTANCE.md)核对 location-only、命名装置、清除格子、
  JSON 往返、地图装置轨，以及真实目标帧 AFA `PauseSkill` 对装置的触发效果。
- 编队绑定容错：用同时包含先锋和其他职业的未保存编队进入人工绑定，确认所有部署栏卡片都出现；
  职业无法分类的卡片可显示“未知”，但必须能够绑定、保存并开始运行。
- 延迟召唤物绑定：按[当前版本实机验收 §7](REAL_MACHINE_ACCEPTANCE.md)完成首次绑定、同局复用、
  重启复用、跨编队复用、删除档案重绑和五类安全中止。
- 战前编队识别：按[当前版本实机验收 §8](REAL_MACHINE_ACCEPTANCE.md)核对真实 OCR、人工修正、F0
  唯一桥接、失败回退、Pending 失效、零输入和成功后才落库。
- 部署方向触控：复现 test2 F483 的“女妖车向上部署”，确认画面完成后日志不再出现负坐标或
  `触控滑动失败`，且 Runner 随后记录同帧动作确认并继续执行 F485。
- 部署后 Resume 稳定性：用 HS-9 重跑 F752 卡缇、F991 安赛尔和 F1586 米格鲁，确认每次日志依次
  出现 `动作确认通过`、`部署后稳定确认通过`、`runtime resume confirmed`，且不中止、不重复 Resume。
- F0 头像桥接性能：HS-9 的真实尺寸测试表明单个标准配对约 1.1 秒、未知别名约 6.3 秒；8 人编队在
  `NeedsBinding` 面板前可能计算逾一分钟。需要单独优化/加入进度反馈，当前不把它误报为绑定 UI 故障。

## 准确下一步

先由用户核对编辑页实时尺子帧的延迟及 E5 跟随开关语义，并复验 HS-9 的三个部署后 Resume 现场；
再执行编辑器 E3 目录搜索和 E4 地图装置技能验收，随后执行 §8 战前编队识别、test2 F483 方向触控、
启动/延迟绑定复验和其他仍 pending 的 GUI 实机项目；
收到实机结果后再记录通过项或按诊断 PNG/JSON、`replicator.log` 定位失败。

## 风险与工作区说明

- AFA 热键只有尺子状态回执，没有 AFA 内部动作回执；部署后新增稳定窗口可降低输入尾阶段吞键概率，
  但真实效果仍以 HS-9 实机复验为准。M9 结论来自用户确认的录像与 CSV 10/10，证据文件未纳入 Git。
- 继续功能完全相信作业和用户对动作前缀的确认；作业与真实场上状态不一致可能导致后续点击错误。
- 作业首动作没有第 60 帧下限；早帧作业仍必须依赖尺子接管、Pause 和动作屏障避免越过目标。
- 根目录 `deploy-scan.png`、`tile-preview-1_7.png` 和 `target/` 是被 `.gitignore` 排除的本地验收/
  构建产物，未纳入提交，也未在本次收尾中删除。
- `examples/hs9.json`、`examples/test2.json`、`examples/test3.json` 是用户本地未跟踪作业，未纳入
  本轮收尾，也未修改或删除。
