# 验收手册

这份文档给的是**你在自己机器上要执行的具体步骤**。我这边只能验到"代码逻辑正确"
（当前全工作区单元测试全绿，含用 10 个真实关卡数据验证的地图投影）；凡是需要游戏在跑、
需要真实触控生效的，都必须由你来做。

本页负责首次安装和完整能力基线。当前开发构建的启动接管、AFA 委托和 10/10 帧精度验收只按
[当前版本实机验收](docs/REAL_MACHINE_ACCEPTANCE.md) 执行，避免旧构建记录与现役步骤混用。

每一节都写明了**通过判据**。有任何一节不通过，**不要跳过去做下一节** —— 后面的
步骤都建立在前面的基础上，带着坐标偏移或读数不稳去跑复刻，只会得到看不懂的失败。

---

## 0. 前置准备（约 15 分钟）

### 0.1 装齐四样东西

| 东西 | 从哪来 | 干什么用 |
| --- | --- | --- |
| 《明日方舟》**PC 客户端** | 官方 | 被操作的对象 |
| **ArknightsCostBarRuler**（尺子） | [Releases](https://github.com/ZeroAd-06/ArknightsCostBarRuler/releases) | 提供绝对帧数，**唯一真源** |
| **Arknights Frame Assistant（AFA）** | 用户自行安装的 AFA | 执行普通暂停/恢复、暂停技能和暂停撤退；复刻器只读配置并触发一次热键 |
| **MAA 的 `resource` 目录** | 已装的 [MaaAssistantArknights](https://github.com/MaaAssistantArknights/MaaAssistantArknights)，或单独下 MaaResource | 关卡地图数据 + 识别模板 |

> 本仓库**不分发** MAA 的资源文件（那是游戏素材，且体积 82MB）。见
> [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md) 的说明。

### 0.2 编译

```powershell
Set-Location -LiteralPath 'D:\Arknights Operation Replicator\replicator'
cargo build --release
```

产物在 `target\release\`：

- `repl-app.exe` —— 主程序（图形界面）
- `frames-probe.exe` / `step-test.exe` / `tile-preview.exe` / `deploy-scan.exe` —— 下面各节要用的验收工具

### 0.3 告诉程序 MAA 资源目录在哪

四选一：

方式 A：启动 `repl-app.exe`，在“作业编辑”页的“MAA 资源”一行点击“选择…”。可以选择
`resource` 本身，也可以选择它的上一级 MAA 目录；界面显示“已加载”后配置已保存。

```powershell
# 方式 B：环境变量（当前终端会话有效，最适合跑验收工具）
$env:REPLICATOR_MAA_RESOURCE = 'D:\MAA\resource'
```

方式 C：在 `repl-app.exe` 旁边的 `config.json` 里填 `"maa_resource_dir"`。

方式 D：什么都不做 —— 程序会从当前目录逐级往上找 `MaaAssistantArknights\resource`。
你现在这个目录结构（三个项目并排）正好能命中，所以多半不用管。

### 0.4 ★必须以管理员身份运行★

明日方舟 PC 端以**更高完整性级别**运行。Windows 的 UIPI（用户界面特权隔离）会把
低权限进程发往高权限窗口的输入**静默丢弃** —— `SendInput` 照常返回成功，游戏却
什么都收不到。表现是"程序跑得好好的，但游戏毫无反应，也没有任何报错"。

所以：**AFA、跑 `step-test.exe` 的终端和 `repl-app.exe` 都必须以管理员身份启动。**
（AFA 的 FAQ 第一条也是这个。）AFA 请先以管理员身份启动，复刻器不会替你启动、提权或重启
AFA；AFA 不可用时也不会回退到旧的 Rust 技能/撤退时序。

怎么确认游戏确实是高权限：

```powershell
(Get-Process Arknights).Path
```

如果**返回空**，说明你的当前权限读不到它的路径 —— 它跑在更高完整性级别上，必须提权。

怎么确认自己是不是管理员：

```powershell
[Security.Principal.WindowsPrincipal]::new([Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole('Administrator')
```

`step-test.exe` 会自己检测并在预检阶段给出提示，但**提前做对能省一轮**。

### 0.5 AFA 委托前置

复刻器把自动开局暂停、普通暂停/恢复、暂停技能和暂停撤退交给**已经运行的 AFA**。它只读配置，
不会写入或纠正 AFA 的设置。启动 AFA 后，用只读命令核对配置文件：

```powershell
$afaIni = Join-Path $env:APPDATA 'ArknightsFrameAssistant\PC\Settings.ini'
Get-Content -LiteralPath $afaIni | Select-String `
  '^(PressPause|ReleasePause|PauseSkill|PauseRetreat|AutoBeginPause|DefaultStrongHoldProtocol)='
```

AFA 当前版本可能把 `Settings.ini` 写成带 BOM 的 UTF-16 LE；复刻器会按 BOM 只读解码
UTF-8、UTF-16 LE 或 UTF-16 BE，不需要也不应为了兼容而转换 AFA 配置文件编码。

以下是本项目验收 profile 的预期值（不是 AFA 源码默认值；首次实机启动时复刻器会把实际读取值
写入日志，复刻器不会替你修改配置）：

```ini
[Hotkeys]
PressPause=g
ReleasePause=Space
PauseSkill=XButton2
PauseRetreat=XButton1

[Main]
AutoBeginPause=1
DefaultStrongHoldProtocol=0
```

如果文件不存在、键缺失、值不同或四个热键有重复，**不要修改 INI，也不要猜测兼容值**；
把 `replicator.log` 和实际配置交给我，先停在预检阶段。键盘热键由键盘 `SendInput` 发送，
`XButton1/2` 和滚轮热键走鼠标事件路径。

启动复刻器后，顶部的「AFA」状态灯必须为绿色并显示「已就绪」。红灯时「开始复刻」
会被拒绝；运行期间 AFA 退出、降权、进程被替换或 `Settings.ini` 指纹变化都会直接中止，
不会静默重发热键。

### 0.6 游戏设置

1. **窗口模式**，分辨率随意（16:9 最省事，非 16:9 也支持，程序会自己算黑边）。
2. 游戏内按键**建议保持默认**。程序会从注册表读你的自定义键位，读失败时会在日志里
   明确告警并回退默认值。
3. 关掉**代理指挥**和**托管**。
4. 显示器刷新率 ≥ 60Hz。

### 0.7 尺子校准

1. 启动 `ruler-app.exe`。
2. 右键悬浮窗 → 校准配置 → `-- 新建 --`。
3. 随便进一关，**保持费用条可见**，点开始校准，等进度跑完。
4. 悬浮窗开始实时显示帧数 = 校准成功。

> **尺子失效的场景**（这些关卡本工具也用不了）：部署费用已满、费用回复被锁定
> （第 15 章那个甩锁链的结晶）、剿灭作战。

---

## 1. 验收：帧源接入

### 做什么

1. 开尺子，进任意一关。
2. 另开一个终端：

```powershell
Set-Location -LiteralPath 'D:\Arknights Operation Replicator\replicator'
.\target\release\frames-probe.exe
```

3. 在游戏里正常打一会儿，然后**按暂停**，再**恢复**。

### 期望输出

```
== ArknightsCostBarRuler 帧源探针 ==
正在连接尺子…
已连接：apiVersion=2 appVersion=v2.3.2 校准配置=profile
开始跟踪，Ctrl-C 退出。
 frame_id=23118    elapsed=724    cycle=  3/30  state=1x_paused    profile=profile   time=00:24:04
 frame_id=23119    elapsed=724    cycle=  3/30  state=1x_paused    profile=profile   time=00:24:04
```

> 这一节已在实机上验证通过（2026-07-26）。
>
> 顺带记录一个上游的坑：尺子文档里的 HTTP 快照接口（`http://127.0.0.1:2606/`）
> 在客户端多发几个请求头时会 RST 掉连接 —— 服务端关 socket 时接收缓冲区还有没读完的
> 请求数据，Windows 就发 RST 而不是 FIN，把已经写出的响应一起冲掉。curl 因为请求头短
> 通常侥幸成功，`Invoke-WebRequest` / ureq 则稳定失败（`os error 10054`）。
> 本工具**不用** HTTP，只走 WebSocket，没有这个问题。

### 通过判据

- [ ] `[连接]` 最终停在 `Connected`
- [ ] 游戏运行时 `elapsed` **单调增长**，不倒退
- [ ] **按下暂停后 `elapsed` 停住不动**，`state` 变成 `1x_paused`
- [ ] 恢复后继续增长
- [ ] 行首**没有 `!`** —— 有 `!` 表示这条读数不可信（费用条被遮挡），偶尔出现正常，
      持续出现要查原因

### 不通过怎么办

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| 一直 `Connecting` | 尺子没开 | 启动 `ruler-app.exe` |
| `profile=<未校准>` | 尺子没校准 | 回 0.7 |
| 满屏 `!` | 鼠标停在费用条上（PC 自绘光标遮挡） | 把鼠标移到屏幕左上角 |
| `elapsed` 长时间不动但 state 是 running | 费用满 / 费用被锁 / 剿灭 | 换一关 |

---

## 2. 验收：单帧脉冲精度 ★最关键的一节★

> 本节记录旧 Rust 直接脉冲的历史诊断方法。现役主复刻已改用 AFA `[Hotkeys]/33ms`，正式验收
> 应运行 `afa-step-test.exe` 并按 `docs/REAL_MACHINE_ACCEPTANCE.md` 核对；`initial_gap_ms` 不再影响主复刻。

这一节决定整个工具能不能用。逐帧推进是**开环定时**（恢复运行固定毫秒数再暂停），
受系统调度抖动影响。**过 0 帧无害**（恢复后立刻暂停，逻辑时间没走，重来即可），
**过 2 帧不可挽回**（游戏没有倒带）。

### 做什么

0. **以管理员身份开一个终端**（见 0.4，不做这一步必然全是空脉冲）。
1. 开尺子（已校准）。
2. 进关卡，**1 倍速**，**按暂停**。
3. 把鼠标移到屏幕左上角别停在费用条上。
4. 在终端跑：

```powershell
.\target\release\step-test.exe 100
```

工具会先做**预检**：单独按一下"进入暂停键"和"退出暂停键"，各等 300ms，
看帧数有没有推进。预检不过就直接退出，不会浪费你 100 次脉冲的时间。

5. 程序会先尝试把游戏拉到前台；拉不动就给你 **5 秒倒计时**，
   在这期间**点一下游戏窗口**。之后还有 3 秒缓冲，然后开始测试。
   **测试期间不要碰键盘鼠标、不要切窗口**（切走会被检测到并立即停止）。

> 按键注入只会发给前台窗口，而你是在终端里敲的命令 —— 所以必须有这个切换步骤。

### 期望输出

```
== 结果 ==
  推进 +0 帧 :   18 次  ##################
  推进 +1 帧 :   82 次  ##############################################

  总脉冲      100
  空脉冲      18 (18%)
  跨帧        0
  最终间隔    24ms

== 判定 ==
  ✓ 通过：没有任何一次跨帧。
    该结果只适用于旧 Rust 诊断，不需要写回主复刻配置。
```

### 通过判据

- [ ] **`跨帧` 必须是 0**。哪怕只有 1 次也算不通过。
- [ ] 没有负数推进（`推进 -1 帧` 之类）
- [ ] `+1` 的次数占比 ≥ 25%（说明脉冲确实在起作用，不是一直空转）

**空脉冲多是正常的。** 实测在 2560×1440 / 垂直同步开的机器上，最优参数也只有
58% 命中 `+1`、42% 空转。这不影响正确性 —— `+0` 完全无害，状态机会自动重试，
代价只是每帧平均要发 1.7 次脉冲（约 100ms）。

### 已知的实测基线

`step-sweep` 在 2560×1440、垂直同步开、`uiScaler=0` 的机器上测得（2026-07-26）：

```
 hold   gap    平均帧    +0    +1   ≥+2
    8    16     0.58   42%   58%    0%   ← 最优
    8    20     0.42   58%   42%    0%
   50    16     0.92   25%   58%   17%
   50    32     1.42   33%    8%   58%
```

两个关键结论，已经固化进默认参数：

1. **`gap` 几乎不起作用。** `hold=8ms` 那组里 `gap` 从 12ms 加到 32ms，
   平均帧数一直在 0.42–0.58 之间纹丝不动。说明游戏是按**渲染帧**轮询输入的：
   两个按键事件落在哪个渲染 tick 上决定一切，tick 内部的毫秒差不可见。
   所以调大 `gap` 没有收益，只增加跨帧风险 —— 上限已从 31ms 压到 24ms。
2. **`hold` 才是元凶。** 对 `hold=50ms` 那组做线性外推，`gap=0` 处的截距是
   **1.0 帧（34ms）** —— 这 50ms 几乎原封不动变成了额外运行时间，
   因为游戏是在**按键抬起**时才响应暂停。默认值已从 AFA 的 50ms 改成 **8ms**。

据此整定策略也改了：**单次空脉冲不再加大间隔**（加了没用，纯增风险），
只有连续 5 次空转才小幅试探 +1ms；一旦跨帧立刻 −3ms。

### 第二轮 100 次实测（hold=8ms，闭环 16–19ms）

```
+0: 65 次   +1: 34 次   +2: 1 次
```

**怎么理解这 1% 跨帧**：跨帧只有发生在最后一步（离目标只剩 1 帧）时才致命，
更早跨帧无害。按此分布每个动作的中止概率 ≈ `p₂/(p₁+p₂)` = 1/35 ≈ **2.9%**，
即 10 个动作的作业一次跑完的概率约 75%。能用但不稳。

据此又加了两层加固（已进默认行为）：

1. **脉冲改为纯自旋定时**。原来两个按键边沿之间用 `Sleep(1)` 粗睡 + 自旋收尾，
   负载下 `Sleep(1)` 可能一觉多睡十几毫秒 —— 正好一个渲染 tick，足以多过一帧。
   现在 gap+hold（≤35ms）全程自旋，消除这个抖动源。
2. **最后一步固定用最短 gap（12ms）**。那是唯一 +2 致命的位置，解暂停窗口越短越安全；
   宁可在最后一格多空转几次。

**根治办法只有一个：把游戏渲染帧率和逻辑帧率对齐（锁 30fps / 关垂直同步）。**
游戏按渲染 tick 轮询输入，60fps 渲染下一个 tick 只有 16.7ms，脉冲落点的量化误差
就是 ±1 逻辑帧；渲染锁到 30fps 后一个 tick = 一个逻辑帧，"解暂停 → 下一 tick → 暂停"
恰好就是一帧，误差源直接消失。改完设置后重跑 `step-test.exe 100` 确认。

### 第三轮 100 次实测（纯自旋定时 + 末步最短 gap）✅ 本节通过

游戏设置未做任何改动，仅靠代码端加固：

```
+0: 65 次   +1: 35 次   ≥+2: 0 次       —— 2026-07-26
```

零跨帧。统计口径上 0/100 与上一轮的 1/100 差异有限，但结合因果机制
（`Sleep(1)` 的超睡正是被消除的抖动源）判断改善是真实的；
第 5 节端到端本身就是几百次脉冲的长跑，会继续验证这一点。

当前性能画像：命中率约 35%，平均每前进一帧约 2.9 次脉冲（≈0.3 秒），
`FRAME_LEAD = 8` 下每个动作的对帧开销约 2–3 秒。嫌慢的话锁 30fps 能显著提升命中率，
但在零跨帧的前提下这只是速度问题，不再是正确性问题。

### 不通过怎么办

**出现跨帧** —— 先别急着调 `initial_gap_ms`。如果间隔已经被自适应压到下限 14ms
却**仍然跨帧**，那就不是间隔的问题，而是存在一段与间隔无关的固定开销。
这种情况直接跑扫描工具，把传递函数测出来：

```powershell
.\target\release\step-sweep.exe 12
```

它会把 `gap`（12–32ms）和 `hold`（8ms / 50ms）各扫一遍，每档 12 次脉冲，
输出一张表：每档实际推进多少帧、`+0`/`+1`/`≥+2` 各占多少、哪一档零跨帧。
最后给出：

- **固定开销有多大**（对平均帧数做线性外推，看 `gap=0` 时的截距）
- **固定开销是不是来自 `hold`** —— 如果 `hold=8ms` 那几档明显更好，
  说明游戏是在**按键抬起**时才响应暂停的，那 50ms 全变成了额外运行时间
- **最佳参数组合**，直接写回 `config.json`

如果**所有档位都做不到零跨帧**，说明抖动本身就 ≥1 帧，调参解决不了。按顺序试：

1. **游戏内把帧率锁到 30**（渲染帧与逻辑帧 1:1，输入轮询就不会错位）——
   这条最可能有效
2. 关掉垂直同步
3. 关掉后台吃 CPU 的程序（浏览器、录屏、杀毒扫描）
4. 游戏改成窗口模式

都没用的话，当前这套"按键脉冲"方案在这台机器上达不到帧精度 ——
这是硬结论，不要将就着往下用。

**出现帧数倒退**：尺子读数不稳。检查鼠标位置、重新校准尺子。

`initial_gap_ms` 只为旧配置兼容保留，不再改变主复刻的 AFA 逐帧行为。

---

## 3. 验收：地图坐标投影

这一节验的是"格子坐标 → 屏幕像素"算得准不准。算错的后果是干员放到隔壁格子，
而且因为格子间距只有几十像素，**肉眼看着差不多，实际全错**。

### 做什么

1. 进一个你知道关卡号的关（比如 1-7），**任意状态都行**（暂停最好，画面稳定）。
2. 终端：

```powershell
.\target\release\tile-preview.exe 1-7
```

3. 打开生成的 `tile-preview-1_7.png`。

### 期望输出

```
MAA 资源目录：D:\...\MaaAssistantArknights\resource
关卡 1-7 (main_01-07#f#) 11×7
客户区 2560×1440，内容区 [0, 0, 2560, 1440]，缩放 2.000，uiScaler 0
已写出 tile-preview-1_7.png（画了 75 个格子）
```

> **"画了 N 个格子"比总格子数少是正常的，不用管。** 这个数只统计投影落在屏幕内的格子，
> 而地图边缘常有一圈纯装饰的 `tile_forbidden`，透视下会被裁到屏幕外。
> 例：1-7 是 11×7 = 77 格，但最后一行 11 格全是不可部署的装饰边，
> 其中两个角投到了屏幕外，所以画出 75 个。全图 39 个**可部署**格子一个没少。
>
> 真正要看的是下面的对齐判据，不是数量。

### 通过判据

打开图片，逐项确认：

- [ ] **绿色十字全部落在格子中心**（正常视角）。允许一两像素误差，**不允许整体偏移，
      更不允许错位一格**。这是本节唯一实质性的判据。
- [ ] **所有可部署的格子**（能站干员的那些）都有绿十字。边缘不可部署的装饰格
      被裁掉不用管 —— 只有能站人的格子才需要点得准
- [ ] 黄色十字（侧视角落点）整体偏下偏左 —— **这是对的**，部署时游戏会压低镜头
- [ ] 品红十字 = 撤退按钮位置，橙色十字 = 技能按钮位置

### 不通过怎么办

| 现象 | 原因 |
| --- | --- |
| 整体偏移固定距离 | 客户区原点算错 —— 换成窗口模式重试，或截图发我 |
| 整体缩放不对（边缘越来越偏） | 窗口不是 16:9 且黑边计算有问题 |
| 完全对不上 | 关卡搞错了。工具会列出相近的关卡名，照着改参数 |

> 这个检查比"和 MAA 的 C++ 实现对拍"更有价值 —— 它验的是与**游戏本身**的一致性，
> 而不是和另一份实现的一致性。

---

## 4. 验收：部署栏识别

### 做什么

1. 进关卡，**暂停**，确保部署栏完整可见、没有弹窗遮挡。
2. 终端：

```powershell
.\target\release\deploy-scan.exe
```

3. 打开生成的 `deploy-scan.png`。

### 期望输出

```
客户区 2560×1440 → 参考帧 1280×720，uiScaler 0，识别耗时 15.6ms

旗标候选（阈值 0.65，按分数降序）：
  [1129, 604, 10, 11]      0.827 ← 采纳
  [1236, 604, 10, 11]      0.774 ← 采纳
  [1063, 606, 10, 11]      0.572
  ...

序号   职业       状态         点击区                      头像哈希
0    先锋       可用         [1084, 610, 75, 110]     3c3c3c3c0c04cdcc
1    先锋       可用         [1191, 610, 75, 110]     fffd7f3902010101

共 2 张卡片，已写出 deploy-scan.png
```

被采纳的候选分数（0.7–0.9）应当和噪声（≤0.6）明显分开。没识别到卡片时，
工具会额外打印子区域推导结果和九个职业模板各自的得分，据此判断是"找错地方"
还是"模板不匹配"。

> 这一节已在实机（2560×1440、`uiScaler=0`）上验证通过（2026-07-26），
> 过程中修掉了三个真实缺陷：
>
> 1. **识别必须先把截图重采样到 1280×720。** MAA 的模板是那个尺度裁的
>    （`BattleOpersFlag.png` 只有 10×11 像素），而归一化互相关没有尺度不变性，
>    直接在 2560×1440 上搜是零匹配。MAA 自己也是先缩图再识别。
> 2. **搜索带横向不能跟着 UI 边距缩放。** PC 端待部署卡片是**右对齐**的，
>    早先横向也乘了 `edge_scale`，带子只到 x=1152，贴右边缘那张卡整个漏掉。
> 3. **职业识别改成"绝对下限 + 相对领先"双条件。** 重采样会抹平边缘，
>    同一枚图标只能拿到 0.60 左右，卡在 MAA 的 0.65 阈值上；
>    但第一名 0.604、第二名 0.171，领先 3.5 倍，本来就该判定为认出。

### 通过判据

- [ ] **卡片数量和屏幕上一致**
- [ ] **职业全部正确**
- [ ] 可用 / 费用不足 / 冷却的状态和屏幕一致
- [ ] 图片里：蓝框横跨整个部署栏；绿框覆盖整张卡；**黄框正好框住干员头像**，
      不能带进卡片边框或职业图标
- [ ] 识别耗时 < 50ms

### 不通过怎么办

先看工具打印的"旗标候选"列表：

| 现象 | 含义 | 处理 |
| --- | --- | --- |
| 最高分 < 0.4 | 模板压根不匹配 | 多半不在战斗界面，或有窗口遮挡 |
| 最高分 0.4–0.65 | 位置对但分数偏低 | 截图可能有缩放损失，把这个分数发我 |
| 候选位置和真实卡片对不上 | 搜索带找错地方 | 检查 `uiScaler`（工具会打印读到的值） |
| 少认几张 | 漏在搜索带之外 | 把 `deploy-scan.png` 发我，看蓝框覆盖到哪 |
| 职业认错 | 图标模板与 PC 端有差异 | 工具会打印九个模板的得分，发我 |

---

## 5. 验收：端到端复刻

前面四节全部通过后才做这一节。

### 5.1 准备作业

复制 `examples\sample-job.json` 改成你自己的：

```json
{
  "stage_name": "main_01-07",
  "frame_replicator": { "version": 1, "speed": "1x", "after_last_action": "pause" },
  "opers": [ { "name": "芬", "skill": 1 } ],
  "actions": [
    { "type": "Deploy",  "frame": 60,  "name": "芬", "location": [3, 2], "direction": "Right" },
    { "type": "Skill",   "frame": 120, "name": "芬" },
    { "type": "Retreat", "frame": 240, "name": "芬" }
  ]
}
```

要点：

- `stage_name` 写 stageId / 关卡号 / 关卡名都行
- `frame` 是**绝对逻辑帧**，1 倍速下 30 帧 = 1 秒
- `location` 用 `tile-preview` 生成的图数格子（左上角是 `[0, 0]`）
- 帧号必须**非递减**；相等表示同一帧内依次执行
- 作业首动作没有第 60 帧下限；`sample-job.json` 的 10/40/60 序列也可用于精确链路验收。
  开局与巡航只按尺子实际读数闭环，若接管或运行期 Pause 已越过下一动作则安全中止
- 每个动作以它自己的最终输入为边界：Deploy 是 Rust 触控拖拽，Skill / Retreat 是把
  目标交给当前鼠标位置后触发一次 AFA 热键；AFA 负责其完整的暂停选中时序
- `Output` 和 `Finish` 都是零输入。动作确认后只有存在下一目标帧时才进入过帧流程，
  没有下一动作就立即结束，不会为收尾补发暂停、恢复或其他热键
- 旧作业里的 `after_last_action: "resume"` 仍可装载，但不再改变上述收尾行为

### 5.2 跑一次

1. 以管理员身份启动 AFA（见 0.4–0.5），再开尺子（已校准）。
2. 启动 `repl-app.exe`，首次会弹免责声明，确认。
3. 顶部三个状态灯都要是**绿的**（尺子已连接 / 游戏已找到 / AFA 已就绪）。
   AFA 灯旁应显示「已就绪」；红灯时开始按钮会被预检拒绝。
4. 点「装载作业」选你的 json。时间轴会列出所有动作。
5. **在游戏里手动把编队排好**（作业里的干员都要带上），进入目标关卡并等待 AFA
   自动暂停；尺子必须显示带 `frame_id` 的可信、战斗内 `1x_paused` 第 0 帧。非零帧的风险确认式
   “继续”另按 `docs/REAL_MACHINE_ACCEPTANCE.md` §5 验收。
6. 点「开始复刻」。开始请求不会发送开局暂停键，也不会自动抢游戏焦点；复刻器会等待你
   手动点击游戏窗口，直到检测到游戏回到前台并收到焦点后的新尺子样本。
7. 焦点后的首样本为 paused 时，若没有完整绑定档案会弹出编队匹配面板；按提示把干员名填到
   对应卡片上，点「确认并开始」。已有完整档案会自动恢复；首样本为 running 时只允许自动
   恢复，缺档案则直接中止，不会在运行中弹人工绑定面板。
8. 编队准备完成后再次手动点击游戏窗口，复刻器会等待最终焦点后的新可信样本，再开始巡航。
   **正式运行期间不要碰键盘鼠标或切换窗口。**游戏焦点等待最多 15 秒，超时直接中止。

到达目标帧后会进入 1 秒动作前尺子屏障：期间持续读取样本，最终只有尺子最新状态仍为目标帧
可信 `1x_paused` 才派发；Session 完成部署识别和坐标准备后，还会在首个拖拽或 AFA 热键前再
复核一次。若尺子已经越帧，任何部署或 AFA 动作输入都不会发出。部署由 Rust 直接注入；普通暂停、
恢复、`33ms` 逐帧、Skill 和 Retreat 由 AFA 执行。逐帧动作前稳定等待 500ms。每个委托动作前都检查游戏前台、AFA 进程/权限和
`Settings.ini` 指纹；任何检查失败都立即中止，**不会调用 SetForegroundWindow 后偷偷重发
热键，也不会回退旧 Rust 技能/撤退时序**。

动作派发后，Runner 会进入 ConfirmingAction 确认屏障：记录派发前最新尺子的
`min_frame_id`，只接受 `frame_id > min_frame_id` 的新样本；同一目标帧上的下一个动作
也会重新记录水位。任何新的运行态（`1x_running`、`0.2x_running` 等）立即失败；
`0.2x_paused` 只能作为等待中的过渡，最终必须收到目标 `elapsed` 的可信 `1x_paused`。
目标帧不符、2x、离开战斗或超时也失败；确认成功前日志只代表“已派发”，不代表动作完成。
单个确认最多等待 2 秒。

### 5.3 通过判据

- [ ] 用户进入关卡后由 AFA 自动停住，点击开始时顶部「绝对帧」不晚于第一个动作帧；Runner
      等待用户手动把游戏切回前台和焦点后的新样本，不发送 `PressPause`，也不自动调用游戏窗口
      `focus()`。仓库示例的 `10/40/60` 序列可直接用于精确验收；首动作没有第 60 帧下限。
- [ ] 编队匹配面板列出的卡片和屏幕一致
- [ ] 时间轴上每个动作依次变绿；变绿表示**已经通过派发后的新样本确认**，不只是
      日志写了“已派发”
- [ ] 顶部“绝对帧”在运行期间与尺子一致，不再显示滞后的 Machine 游标；若尺子已越过目标，
      必须在对应 Deploy/Skill/Retreat 输入日志出现前中止。
- [ ] 完成后状态栏显示 `复刻完成。脉冲 N 次（空脉冲 X，跨帧 0）`
- [ ] **跨帧必须是 0**
- [ ] **最后一个动作确认后，游戏仍处于暂停态，并停留在最后动作的目标帧。**技能或撤退
      的日志显示成功不算通过；必须以游戏画面和尺子 `1x_paused` 状态为准。
- [ ] `Finish` 收尾没有任何补发输入；旧作业的 `after_last_action` 不得改变这一点。
- [ ] 普通暂停/恢复、Skill 最后动作、Retreat 最后动作分别至少成功一次；每个动作的
      AFA 热键只发送一次，失焦或 AFA 检查失败时直接中止且不盲重试。
- [ ] 每个巡航运行区间只有一次 `dispatching AFA runtime resume`；随后必须先出现
      `runtime resume confirmed`，才能继续按新尺子帧推进。积压 paused 样本不得触发第二次 Resume。

> **当前结论（2026-07-30）：代码和自动门禁已完成，M8 功能与运行链路已通过；用户已确认当前
> AFA 逐帧构建的 M9 录像与 CSV 10/10 通过。** 当前二进制身份、启动路径、运行期日志判据和表格以
> [当前版本实机验收](docs/REAL_MACHINE_ACCEPTANCE.md) 为准；旧日志不能替代。

### 5.4 帧精度核验（真正的验收）

只有 5.3 的最后动作已经确认在目标帧 `1x_paused`，才进行录像核验。上面只证明"流程
跑通了"，不证明"动作确实落在目标帧"。要证明后者：

1. 用尺子的 `ruler-recorder` 同步录制：

```powershell
Set-Location -LiteralPath '<尺子目录>'
cargo run --release -p ruler-recorder -- -c config.json -o recordings -d 60
```

2. 跑一次复刻。
3. 事后逐帧回看录像 + CSV，确认每个动作的**生效帧**和作业里写的 `frame` 一致。

**重复 10 次，判据：10/10 帧号完全一致。**

> 允许有 ±0 帧的差异 —— 这就是"帧级"的含义。如果稳定差 1 帧，那是系统性偏移，
> 可以在作业里整体补偿；如果时好时坏，说明脉冲不稳，回第 2 节重调。

### 5.5 失败时记录什么

以下情况都应立即停止本次运行，不要重试同一个热键：

| 现象 | 含义与处理 |
|---|---|
| AFA 状态灯变红、AFA 退出或提示降权 | 委托来源不可用；保存日志并重新以管理员身份启动 AFA 后再开始 |
| `Settings.ini` 被修改、键缺失或热键重复 | 配置指纹/协议不再是预检时的版本；不要现场编辑，保存实际 INI 和日志 |
| 动作确认收到 `1x_running`、`0.2x_running` 等运行态 | 游戏可能已越过目标帧；立即中止，不补发 Pause |
| 动作派发前尺子帧已大于目标帧 | Machine 游标已滞后；必须在任何动作输入前中止，以尺子显示为准 |
| Pulse 日志出现单条 `deferred ... paused` 后又出现 running | 第一条 paused 是旧管线候选；继续等待最终 paused，期间不得进入动作或补发下一脉冲 |
| 运行期 Pause 出现多条相同 elapsed 的 running 样本 | 只是尺子重复分析；`advanced` 应保持 0，不得按样本条数判 `PauseIneffective` |
| Resume 后出现多条新 frame_id 的 paused/unknown 样本 | 是派发前进入尺子管线的积压画面；应记录 waiting 并继续等 running，不能重复发送 `ReleasePause` |
| 确认收到 `0.2x_paused` | 这是唯一允许等待的过渡样本；继续等新的可信 `1x_paused`，不要把它记为完成 |
| 确认收到目标帧以外、2x、离战或超时 | 动作无法证明落在目标帧；立即中止，不盲重试 |
| 游戏不在前台 | 前台检查失败直接中止；只有编队面板关闭后允许一次恢复前台的尝试，动作委托阶段不自动抢焦点 |
| 接管或运行期 Pause 后尺子读数已越过目标 | 保存日志并安全中止；以尺子实际读数为准，不派发已经错过的动作 |

把以下材料一起保存：完整 `replicator.log`、AFA 配置中上述七项的实际值、尺子状态和
最后一个 `frame_id`。日志中的“已派发”不能替代画面和尺子上的动作确认。

---

## 6. 验收：资源占用



复刻期间开任务管理器（或 `Get-Process repl-app`）观察 5 分钟。

### 通过判据

- [ ] `repl-app.exe` 私有工作集 **< 60MB**
- [ ] 空闲时 CPU **< 0.5%**
- [ ] 复刻中 CPU **< 5%**（不含尺子进程；尺子自己会占一些，那是它在持续截图分析）

```powershell
Get-Process repl-app | Select-Object Name,
    @{n='内存MB';e={[math]::Round($_.PrivateMemorySize64/1MB,1)}},
    @{n='CPU秒';e={[math]::Round($_.CPU,1)}}
```

---

## 7. 已知限制

这些不是 bug，是当前版本明确不做的事：

| 限制 | 原因 |
| --- | --- |
| **只支持 1 倍速** | 变速会同时改变脉冲时长和尺子的帧率假设，首版不冒险。作业里含 `SpeedUp` / `BulletTime` 会在装载时直接报错 |
| **不支持费用满 / 费用锁定 / 剿灭作战** | 尺子靠费用条推算帧数，这些场景下费用条不动 |
| **复刻期间不能碰键鼠、不能切窗口** | 键盘注入发给前台窗口；切走就打到别的程序上了 |
| **游戏窗口尺寸不能变** | 变了所有坐标映射作废，程序会直接中止而不是硬撑 |
| **干员名靠你一次性绑定** | 不带 OCR 和头像库（省掉约 240MB）。同一套编队第二次进关会自动恢复绑定 |
| **不支持代理指挥 / 托管** | 暂停键会失效，程序检测到会中止 |
| **自动触发的技能会静默无效** | `Skill` 动作是"选中干员 → 按技能键"。若该技能是**自动触发**的，按键不产生任何效果，但程序**无从判断、不会报错**，时间轴照样显示成功。作业里只写手动触发的技能 |
| 同一关卡号可能对应多个 stageId | 主线关几乎都有 `#f#` 变体（`main_01-07` / `main_01-07#f#`），`code` 都是 `1-7`。已核对全部 686 组配对**地图数据完全一致**，取哪个都对；日志出现歧义告警且投影明显不对时，改用精确 stageId |

---

## 8. 出问题时怎么给我信息

按这个顺序收集，能大幅缩短来回：

1. **哪一节不通过**（第 1–6 节的编号）
2. **工具的完整输出**（终端里全选复制）
3. **生成的图片**（`tile-preview-*.png` / `deploy-scan.png`）
4. **详细日志**：GUI 自动把日志写到 exe 旁边的 `replicator.log`
   （每次启动覆盖，含每条被拒样本的逐条记录和超时瞬间的尺子快照现场）。
   出问题直接把这个文件发过来，不需要设任何环境变量。

5. **环境**：Windows 版本、显示器分辨率与刷新率、游戏窗口分辨率、
   游戏内 UI 缩放设置、尺子版本

---

## 附：各节对应的实现

方便你出问题时定位，也方便以后改代码时知道该重跑哪一节验收。

| 验收节 | 主要代码 |
| --- | --- |
| 1 帧源 | [repl-frames](crates/repl-frames/src/) |
| 2 脉冲 | [stepper.rs](crates/repl-input/src/stepper.rs)、[stepping.rs](crates/repl-core/src/stepping.rs) |
| 3 投影 | [tile.rs](crates/repl-core/src/tile.rs)、[viewport.rs](crates/repl-core/src/viewport.rs) |
| 4 识别 | [repl-vision](crates/repl-vision/src/) |
| 5 端到端 | [machine.rs](crates/repl-core/src/machine.rs)、[session.rs](crates/repl-app/src/session.rs)、[runner.rs](crates/repl-app/src/runner.rs) |

## 当前开发构建增量验收

启动接管、已有/人工绑定、running 负向路径、运行期 Resume/Pause/Pulse 和录像 CSV 10/10 的现役
步骤统一维护在 [当前版本实机验收](docs/REAL_MACHINE_ACCEPTANCE.md)。本页不再复制第二套步骤。
