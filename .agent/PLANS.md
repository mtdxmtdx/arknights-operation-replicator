# 项目执行计划

## 当前阶段：编辑页 MAA 资源路径选择（2026-08-10）

### 目标与完成标准

用户可在“作业编辑”页选择 MAA `resource` 或其上一级 MAA 目录。程序先验证并加载 Tile Pos 索引和
干员目录，成功后原子保存 `maa_resource_dir`，在当前窗口热刷新逻辑地图与干员搜索；取消、无效目录
或保存失败保留旧配置和旧运行时资源，整个过程只访问本地文件且保持编辑模式零输入。

### 里程碑

- [x] MR0：确认旧实现只有配置/环境变量/父目录自动发现，设置按钮只显示路径，没有文件夹选择入口。
- [x] MR1：先增加 resource/上级目录归一化和无效目录拒绝测试，观察缺少实现的红灯。
- [x] MR2：增加编辑页资源路径、状态和 Win32 文件夹选择器。
- [x] MR3：地图索引与 `battle_data.json` 同时加载成功后原子保存，并热替换地图/干员搜索资源。
- [x] MR4：同步 README、编辑器说明、安装验收、编辑器验收与项目规则。
- [x] MR5：版本提升至 `0.2.0`；262 项 workspace 测试、Clippy、release、格式和 diff 门禁通过。
- [ ] MR6：用户按 `docs/EDITOR_ACCEPTANCE.md` E0 验收文件夹对话框、取消、持久化、热刷新和失败保留。

### 验证结果

- `target\release-v0.2.0\release\repl-app.exe`：34,513,920 字节，SHA-256
  `6BB8CD2DD6E3F1748CE0C31C96CE49519D5595E2C5F23295A31D0741C924BCC4`。
- 定向测试锁定 `resource`/MAA 上级目录两种输入、缺少 Tile Pos 拒绝、UI 回调、配置保存调用与状态展示。
- 自动测试不能操作系统文件夹对话框或证明视觉布局，MR6 继续由用户实机完成。

## 当前阶段：编辑页实时尺子绝对帧（2026-08-04）

### 目标与完成标准

尺子连接时，作业编辑页始终显示最新累计绝对帧；关闭“跟随尺子”只冻结人工时间轴光标，不隐藏或
冻结实时尺子读数。显示刷新延迟控制在约一帧 UI 周期，同时不提高 AFA、窗口扫描或输入路径频率。

### 里程碑

- [x] EFR0：确认编辑模式已读取尺子，但旧实现只在 400ms 状态定时器中刷新且没有独立显示属性。
- [x] EFR1：先增加编辑页实时帧存在性和刷新周期回归，观察红灯。
- [x] EFR2：增加独立 `editor-ruler-frame` UI 属性和“尺子绝对帧”显示。
- [x] EFR3：把实时帧拆为 33ms 轻量定时器；400ms 重状态轮询保持不变。
- [x] EFR4：259 项 workspace 测试、Clippy、release、格式与 diff 门禁通过。
- [ ] EFR5：用户核对实际显示延迟、断连提示以及跟随开/关时的光标语义。

### 验证结果

- `target\release\repl-app.exe`：34,479,104 字节，SHA-256
  `9C642D0046C87C7567E3F5C12037B6833A900AD55239ED3F57D0BB203415B54A`。
- 编辑模式仍只读取 `RulerClient` 共享快照；没有新增 AFA 探测、Session、窗口定位或输入调用。

## 已实现、待实机验收：部署后 Resume 稳定屏障（2026-08-03）

### 目标与完成标准

修复 HS-9 中 F752、F991、F1586 部署动作已经完成并通过尺子确认，但随后一次 `ReleasePause` 未使
游戏恢复、两秒后停止的问题。非最终 Deploy 在下一输入前必须持续保持 500ms 的目标帧可信暂停，
Resume 仍只派发一次；真正未恢复时给出准确诊断。

### 里程碑

- [x] R0：用现役日志重放确认三次均为动作确认成功、Resume 后连续 119–121 条可信 paused 样本。
- [x] R1：先建立部署稳定窗口与 Resume 专用超时原因的红灯测试。
- [x] R2：实现 500ms 权威暂停稳定窗口，保留 ADR-0005 的单次 Resume 事务约束。
- [x] R3：完成 258 项 workspace 测试、Clippy、release、格式和 diff 门禁，并同步 ADR/状态/验收。
- [ ] R4：用户在 HS-9 复验 F752、F991、F1586，确认三次均出现稳定确认和 running 回执。

### 验证结果与边界

- 自动回归覆盖完整等待时长、窗口内 running 安全拒绝和 `ResumeIneffective` 错误分流。
- `target\release\repl-app.exe`：34,466,816 字节，SHA-256
  `1071E6084977F617476A95992FBA36DBDE9B9EC43E6C49E1A6CDED291D810FD2`。
- 自动测试验证状态与时序门禁；AFA/游戏是否仍会吞掉热键由 R4 实机结果确认。

## 已实现、待实机验收：编队干员目录搜索（2026-08-02）

### 目标与完成标准

编辑器的“编队干员”输入框读取用户本地 MAA `battle_data.json`：输入名称片段时展示有限、去重的
候选列表，点击即可加入 `opers`；已加入名称不重复展示。MAA 资源缺失时继续允许手动输入。整个路径
只读取本地资源，不初始化 OCR、不截图、不访问 AFA 或游戏窗口。

### 里程碑

- [x] O0：确认现有 `OperatorCatalog` 已加载 MAA 干员目录，编辑器缺少搜索 UI/回调。
- [x] O1：增加目录名称搜索（前缀优先、包含匹配、去重、上限）和编队输入候选列表。
- [x] O2：候选点击写入现有编队编辑历史；手动输入和重复过滤保持原有语义。
- [x] O3：运行 workspace test、Clippy、release、格式与 diff 门禁，更新构建身份。
- [ ] O4：用户完成编辑器搜索手感、目录缺失回退与严格零输入实机验收。

### 已知关联

- 该功能修正作业中容易出现的别名/错别字（如“安塞尔”应选择标准目录名“安赛尔”），但不改变旧作业
  或自动重命名现有 `opers`。
- 它不解决 F0 头像桥接的性能问题；该问题另行诊断和改造，避免把 UI 搜索改动扩大为运行期行为变化。

### 自动验证结果

- `cargo test --workspace`：255 项通过；新增目录搜索去重/排序测试和编辑器搜索 UI 回归。
- `cargo clippy --workspace --all-targets -- -D warnings`、`cargo build --release`、
  `cargo fmt --all -- --check`、`git diff --check`：通过。
- `target\release\repl-app.exe`：34,463,232 字节，SHA-256
  `EE1B9176FEED01AEAAED8424DE7493F9F4B917CDA3B92D0A610AF51EA72B4E44`。

## 已实现、待实机验收：地图装置技能编辑（2026-08-02）

### 目标与完成标准

编辑器允许用 MAA 兼容的 `Skill + location` 描述关卡预置装置：技能动作可显示并点选逻辑地图，
名称可选，显式格子优先；普通干员技能可以清除格子并恢复按部署记录定位。预置装置不得进入部署栏
扫描、延迟绑定或头像档案。

### 里程碑

- [x] S0：确认 Session/AFA 运行链路已经支持显式 location，缺口只在编辑与严格校验边界。
- [x] S1：技能属性面板开放 X/Y 和地图点选，增加清除格子入口及地图装置轨。
- [x] S2：允许 location-only Skill，并允许未 Deploy 的命名装置凭显式 location 通过严格编译。
- [x] S3：补充 Copilot、JobDocument、UI 回归及使用/验收文档。
- [x] S4：运行完整自动门禁并生成新的 release 身份。
- [ ] S5：用户按 `docs/EDITOR_ACCEPTANCE.md` E4 完成装置技能 GUI 与真实 AFA 动作验收。

### 决定

- 不新增私有动作类型；保存格式保持 MAA `Skill`，用 `location` 区分显式格子目标。
- 名称只是可选标签；无名称但有格子的 Skill 合法并进入“地图装置”轨。
- 显式 location 直接走现役 `resolve_target` 和 AFA `PauseSkill`，不扩大 Rust 输入边界。

### 自动验证结果

- `cargo test --workspace`：253 项通过；`repl-core` 122 项、主程序回调 12 项，新增覆盖命名装置、
  location-only Skill、JSON 往返、无误报诊断和 UI 格子/清除模式。
- `cargo clippy --workspace --all-targets -- -D warnings`、`cargo build --release`、
  `cargo fmt --all -- --check`、`git diff --check`：通过。
- `target\release\repl-app.exe`：34,337,280 字节，SHA-256
  `29039E43D51055E58C1060ED3AE7FB529C71F042E8F16471F6500F014FEDE280`。
- S5 的地图点选手感、真实装置技能效果和 AFA 行为仍由用户实机验收。

## 已实现、待实机验收：MAA 战前编队识别与 F0 头像桥接（2026-08-02）

### 目标与完成标准

在编队确认界面提供显式、只读的“扫描编队”：复用 MAA 姓名锚点、PaddleOCR 识别模型与
`battle_data.json`，由用户确认识别结果；进入可信第 0 帧后，再把编队大头像与真实部署栏头像做
职业约束、多尺度 NCC 桥接。唯一匹配才写入现有全局头像档案和编队 profile，失败或歧义继续走
人工绑定。扫描、确认和失败回退均不得发送 AFA、鼠标、触控或键盘输入。

### 冻结边界

- 使用程序自带 ONNX Runtime 执行用户 MAA `resource/PaddleOCR/rec` 模型；不链接 `MaaCore.dll`，
  不读取 MAA DLL，不随仓库分发模型、模板或游戏资源。
- 名称解析先匹配作业 `opers`，再用 MAA 全干员目录提示编队不一致；只有用户确认的作业干员进入
  Pending，非作业名称不写档案。
- 已有全局头像和编队 profile 优先；Pending 只处理仍未绑定的名称和卡片，绝不覆盖既有档案。
- Pending 只用于下一次同作业、同窗口、同尺寸且未超时的 `RunMode::FromZero`；“继续”、召唤物和
  地图装置保持现役延迟绑定语义。
- 自动门禁由本会话完成；真实编队、OCR、零输入和 F0 桥接效果由用户按验收手册实机确认。

### 里程碑

- [x] F0：实现 MAA OCR 资源校验、姓名锚点、整行 CTC OCR、目录解析和纯逻辑测试。
- [x] F1：实现只读扫描诊断工具与扫描结果确认 UI，锁定异步 generation 和 Pending 生命周期。
- [x] F2：实现 F0 多尺度头像桥接、既有绑定优先级和批量持久化。
- [x] F3：补齐资源错误、歧义、迟到回调、过期 Pending 和零输入架构回归。
- [x] F4：运行 workspace test、Clippy、release、格式和 diff 门禁，更新状态、ADR 与实机验收。

### 验收判据

程序在简体中文编队确认界面能给出可人工修正的识别结果；确认后，F0 唯一桥接的可见干员不再
弹人工绑定，未桥接部分仍正常弹窗。错编队只警告不落库；取消、资源损坏、窗口/作业变化和超时
不留下 Pending 或部分档案。扫描路径的依赖层不可见任何输入接口。实机验收由用户完成，自动测试
不能把合成图或模型 smoke 描述为真实 OCR 已通过。

### 自动验证结果

- `cargo test --workspace`：248 项通过，包含真实 MAA OCR 资源/ORT 初始化、CTC、名称解析、桥接唯一性、
  异步 generation、零输入依赖和既有运行链路回归。
- `cargo clippy --workspace --all-targets -- -D warnings`、`cargo build --release`、
  `cargo fmt --all -- --check`、`git diff --check`：通过。
- `target\release\repl-app.exe`：34,304,512 字节，SHA-256
  `DDDF66FB6DB4ED083DD73A8B7634B9909785151B2EA7FF9601D64CDB2A78BF7F`。
- `target\release\formation-scan.exe`：20,904,960 字节，SHA-256
  `0F63999D98CA8BA1E150173997E7A56DF6B145A166F18EB34B807B2AA71422D4`。
- 真实编队 OCR、UI 手感、零输入观察和 F0 桥接仍由用户按 §8 验收，不在自动通过范围内。
- 源码、文档、规则和验收入口已完成知识收尾并同步 GitHub `develop`；项目没有独立部署环节。

### 2026-08-02：Windows 部署方向触控越界修复

- [x] 实机日志确认 F483 的部署拖拽已经落地，但随后 `facing Up` 路径生成负坐标并被
  `InjectTouchInput` 拒绝，因此 Runner 未登记动作完成；不是尺子确认误判。
- [x] 保留 MAA 的有限整段平移，再把剩余越界端点限制到 Windows 客户区有效像素内；直线插值的
  全部触点均保持在窗口中，方向不变，必要时只缩短朝向滑动距离。
- [x] 增加 2560×1440、1-7 `(7, 2)` 向上部署的实机坐标回归，以及四方向边缘回归；局部和完整
  自动门禁通过。修复由独立本地提交 `d8ad54b` 跟踪，可单独 `git revert d8ad54b`。

## 已实现、待实机验收：延迟人工绑定与全局头像复用（2026-07-31）

### 目标与完成标准

召唤物、地图装置等开局不在部署栏的目标不再阻止开始运行。第一次 Deploy 到达目标帧后，程序在
保持可信 `1x_paused` 的前提下扫描部署栏；已有全局头像档案时自动匹配，否则弹出一次人工绑定。
绑定过程零游戏输入，用户切回游戏后必须取得新的同目标帧尺子样本才能部署。保存后的头像同局、
同编队和跨编队复用。

### 冻结边界

- 启动人工绑定允许部分完成；未绑定名称只在首次实际 Deploy 时成为强制要求。
- 全局头像档案不进入编队指纹，继续复用现有 `BindingStore.avatars`；第一版每个名称保存最新头像。
- 延迟绑定只发生在动作前稳定屏障已经确认的目标帧，弹窗、保存与等待焦点期间不调用 AFA、触控或
  逐帧；返回后要求更新的可信 `1x_paused` 样本仍等于目标帧，否则安全中止。
- MAA `opers` 继续表示开局编队；由 Deploy 首次引入的动作目标允许不在 `opers` 中，技能/撤退可在
  该 Deploy 之后引用。多个同名目标的技能/撤退仍推荐写 `location`。
- 运行期只做头像匹配和人工命名，不引入 OCR、全干员头像库或自动猜测。

### 里程碑

- [x] D0：补齐领域模型、MAA 召唤物兼容校验和纯逻辑回归。
- [x] D1：启动绑定允许部分完成，并在会话建立时恢复全局 Deploy 头像档案。
- [x] D2：实现目标帧延迟绑定、焦点交还、新尺子水位与越帧安全中止。
- [x] D3：编辑器允许维护召唤物/装置目标，并与编队干员共同进入动作下拉。
- [x] D4：同步 ADR、状态和实机验收，运行 workspace test、Clippy、release、格式与 diff 门禁。
- [ ] D5：用户按本地 test2 作业完成首次绑定、同局复用、重启复用、跨编队复用和异常中止实机验收。

### 验收判据

第一次出现只弹窗一次；绑定前后绝对帧不变；取消、running、非 1x、离开战斗或越过目标均零部署
中止。档案存在且匹配时不得弹窗；删除对应档案后必须重新绑定。自动测试只能证明状态、持久化和
输入门禁，不能替代用户对真实卡片、焦点切换和画面动作的确认。

### 当前发现与决定

- `BindingStore.avatars` 已经是名称到最新头像的全局档案；延迟目标只需复用它，不应写入编队 profile。
- 历史目标头像同时命中多卡时，候选过滤必须忽略该目标自身的旧头像，否则歧义卡会被错误隐藏。
- 开局 running 样本不能弹窗，但缺少的开局目标可以安全延迟到实际 Deploy；目标帧绑定则必须是
  paused 人工链路，并持续监控尺子是否恢复运行或离开目标帧。

### 自动验证结果

- `cargo test --workspace`：通过；核心模型 116 项测试，含 Deploy 首次引入与延迟目标持久化回归。
- `cargo clippy --workspace --all-targets -- -D warnings`、`cargo build --release`、
  `cargo fmt --all -- --check`、`git diff --check`：通过。
- release 身份：`target\release\repl-app.exe`，15,117,312 字节，SHA-256
  `E8D4E2F143B4B8FA12314EEF131F20E2380A605618F756AA000AAC0F8B21B079`。
- D5 仍由用户实机执行；自动门禁不把头像识别和真实焦点交接描述为已验收。

### D4.1：启动可见绑定修复（2026-07-31）

- [x] 用实机 `replicator.log` 和未更新的 `bindings.json` 把故障收敛到启动绑定事务；日志连续停在
  “准备编队绑定”，没有确认收到、保存或最终焦点阶段。
- [x] 启动是否弹窗改为同时要求“作业仍有未绑定名称”和“部署栏确有未识别可见卡片”；不可见的
  后续目标不再让每次启动重复弹窗。
- [x] 可见卡片改用作业 `opers` 中尚未绑定名称的下拉菜单；同一名称重新选择时自动解除旧卡选择。
- [x] 确认按钮只提交事务，不提前关闭面板；工作线程校验并保存成功后发送 `BindingCommitted`，UI
  才关闭面板并明确提示手动点击游戏窗口继续。
- [x] 文件日志增加“UI 响应已收到、绑定已保存、运行失败”事务边界，避免保存失败只留在 UI。
- [x] 定向回归覆盖确认桥只消费一次、不可见名称不重复弹窗、确认前不关闭和编队下拉。
- [x] 完整自动门禁通过；release 为 15,124,992 字节，SHA-256
  `44BEE136F47C18FFB69F73C0DC1CF372FA2EFEA5CB508A2AAC3E230F770DAFD0`。

## 已完成阶段：主复刻逐帧委托 AFA 与 M9（2026-07-30）

### 目标与完成标准

主复刻不再用 Rust 的恢复/暂停本地定时脉冲，统一调用 AFA `[Hotkeys]/33ms`（用户当前绑定 R）。
尺子结算屏障和跨帧安全中止保持不变；逐帧前稳定等待从 1000ms 减到 500ms，目标帧动作前屏障从
2000ms 减到 1000ms。AFA 缺少 `33ms` 时必须在预检阶段失败关闭。

- [x] 将 `33ms` 从诊断可选热键升级为主程序必需热键，并覆盖缺失/重复/解析回归。
- [x] Session 的主 Pulse 改为单次 `AfaAction::StepOneX`，移除主路径 `PauseController`。
- [x] Machine 的 Pulse 命令移除本地 gap 参数；`initial_gap_ms` 仅保留旧配置兼容。
- [x] 将逐帧前与动作前稳定等待分别缩短为 500ms 和 1000ms，确认超时保持 2 秒。
- [x] 新增 ADR-0010，并同步规则、领域语言、README、验收和当前状态。
- [x] 运行 workspace test、Clippy、Release build、格式与 diff 门禁；标准路径构建 SHA-256 为
  `09EEED203464B64D55F5FA53803006B7BB827EAF35648888E42D58A0D064AB6D`。
- [x] 2026-07-30 用户确认当前哈希按验收手册完成 M9 录像与 CSV 10/10；证据文件未纳入 Git。

## 已实现、待实机验收：编辑器确认式输入与受控选择（2026-07-30）

### 目标与完成标准

编辑器内会写入作业历史的文本/数字输入只在回车或失焦时提交一次，不再按每个字符生成撤销记录；
动作干员从已维护编队选择，部署方向从上、下、左、右、无方向选择，避免自由文本与作业枚举不一致。

- [x] 用源码调用点回归稳定捕获绝对帧 `edited` 每键提交问题，确认旧实现为红。
- [x] 增加 `CommitLineEdit` 草稿/提交边界；标题、关卡、帧数、X/Y 和说明在回车或失焦提交。
- [x] 干员下拉仅使用“请选择干员”占位和当前编队；注释动作不显示干员选择。
- [x] 部署方向下拉限定上、下、左、右、无方向，并回归验证中文值与内部枚举映射。
- [x] 回归覆盖确认 249 后一次撤销回到旧帧、再撤销删除新动作；动作切换仍刷新字段。
- [x] 运行 workspace test、Clippy、Release build、格式和 diff 门禁。
- [ ] 用户实机确认回车/失焦提交、撤销粒度和两个下拉菜单。

## 已实现、待实机验收：多职业编队绑定容错（2026-07-30）

### 目标与完成标准

人工绑定依赖部署栏公共旗标枚举卡片；职业只作界面提示。某个职业图标与 MAA 安卓模板不匹配时，
不得把已经命中旗标的整张卡删除，必须以“未知”职业保留，允许用户完成头像与名字绑定。

- [x] 建立“旗标命中、职业九选一失败时绑定卡片被删除”的确定性红测。
- [x] 职业失败降级为 `Role::Unknown`，保留卡片点击区和头像，不改变普通头像跟踪阈值。
- [x] 定向回归由红转绿；九职业模板仍完整加载，UI 不按职业过滤卡片。
- [x] 运行全工作区测试、Clippy、release 构建、格式和 diff 门禁。
- [ ] 用户用包含先锋及其他职业的未保存编队确认所有卡片可见且可绑定。

## 已完成阶段：动作属性切换隔离（2026-07-30）

### 目标与完成标准

修复动作属性输入框在用户编辑后脱离模型绑定、切换动作仍保留上一动作输入值的问题。选择任意
动作时，属性面板必须始终显示该动作自己的帧数、干员、坐标、朝向和说明，不得把前一动作的值
带入下一动作。

- [x] 建立可稳定复现“先输入 123、再切换到值为 40 的动作仍显示 123”的 Slint 回归测试。
- [x] 先用双向绑定隔离动作值；随后由当前阶段的 `CommitLineEdit` 显式同步与受控下拉取代，继续
  覆盖帧数、干员、X/Y、朝向和说明。
- [x] 定向回归由红转绿，`cargo check -p repl-app` 通过。
- [x] 运行 workspace test、Clippy、Release build、格式和 diff 门禁。
- [ ] 用户在实际编辑器中确认不同动作间属性不串值。

## 已实现、待实机验收：复刻日志滚轮浏览（2026-07-30）

### 目标与完成标准

把复刻执行页的日志从固定文本框改成明确、可发现的运行控制台。长日志必须形成真实的垂直内容
范围；鼠标位于日志正文上即可滚轮浏览，右侧滚动条仍可拖动，短日志不产生无意义滚动。

- [x] 保持现有深色工业 HUD 风格，增加“运行日志”标题和滚轮提示。
- [x] 显式绑定 ScrollView viewport 高度到换行文本首选高度，确保长文本形成滚动范围。
- [x] 增加正文滚轮命中区并限制 `viewport-y`，同时给滚动条保留独立拖动区域。
- [x] 运行 workspace test、Clippy、Release build、格式和 diff 门禁。
- [ ] 用户实机确认滚轮方向、长日志浏览和窗口缩放体验。

## 已完成阶段：M8 验收与本地知识收尾（2026-07-29）

### 目标与结果

把用户已完成的 M8 功能与运行链路实机验收写入唯一现役状态，同步 README、项目规则、验收手册
和历史计划中的过期说法；完整审计代码、文档、规则、记忆边界和工作区残留后，将本轮编辑器、
继续功能、M8 链路及知识收尾统一提交到本地 `develop`，不上传远端。

- [x] 用户确认 M8 §3–§4 全部通过；记录当前二进制身份和验收范围，不虚构未入库的日志/截图。
- [x] README、AGENTS、ACCEPTANCE、REAL_MACHINE_ACCEPTANCE、EDITOR_ACCEPTANCE、
  CURRENT_STATE 和本计划对齐；在该阶段结束时，M9、继续实机、编辑器 GUI 仍为 pending；M9 后于
  2026-07-30 由用户确认通过。
- [x] 机械枚举 Markdown、规则链、分支/worktree、dirty 文件和忽略产物；Codex 生成记忆保持只读。
- [x] 运行项目规定的测试、Clippy、Release build、格式与 diff 门禁。
- [x] 将审计后的全部本地改动提交到本地 `develop`；没有 push、PR 或远端写入。

### 阶段终态

M8 为 `real-machine verified`；代码与知识为本地已验证并闭环。M9、风险继续实机验收和编辑器 GUI
验收不属于本次完成范围。根目录被忽略的验收截图和 `target/` 保留为清理候选，未获最终汇报后的
明确确认前不删除。该阶段先创建本地提交 `2e0292f`；用户随后明确要求上传，现已同步到
`origin/develop`。

## 已完成阶段：风险确认式“继续”（2026-07-29）

### 目标与成功标准

在可信战斗内 `1x_paused` 的任意非零帧允许用户冻结“继续截止帧”，明确承担截止帧及以前动作
未经程序验证的风险，再用一台全新的 `Runner` / `Machine` 执行第一个更晚动作及全部后续动作。
第 0 帧仍只使用“开始复刻”；已无后续动作、样本不可信、AFA 未就绪或确认后冲过下一动作帧时
必须拒绝，不得静默扩大截止帧。

以 `examples/test1.json` 为主要例子：F11 继续时将 F10 桃金娘部署标记为“用户确认完成”，推导
桃金娘位于 `(3,2)`，只从部署栏绑定风笛，并从 F40 桃金娘技能继续。F1 继续允许在未跳过动作的
情况下承担冲过第 0 帧的风险。

### 冻结边界与决定

- 截止帧在点击“继续”时冻结；所有 `frame <= cutoff` 的同帧动作整体视为用户确认完成。
- 确认弹窗展示跳过动作、推导场上状态、下一动作、头像档案缺口和风险；确认前零输入。
- 确认时重验 AFA 和尺子；最终接管必须满足 `cutoff <= elapsed <= next_action.frame`。
- 继续创建新会话，不恢复旧线程；部署写入、撤退移除，以作业为权威推导场上状态。
- 只绑定后续需要部署且当前不在场的干员。当前在场但后续会重新部署的干员必须已有头像档案，
  缺档案时阻止确认；不增加运行期延迟绑定。
- “用户确认完成”是独立于尺子动作确认的状态；已由尺子确认的行保持原状态。继续运行完成后显示
  “带风险完成”，不能计入 M9 精确复刻结果。

### 里程碑

- [x] C0：冻结领域决定、test1 场景和接管安全区间。
- [x] C1：实现并测试 `ContinuationPlan`、动作前缀生命周期回放和续跑 Machine 起点。
- [x] C2：接入新 Runner 会话、推导战场播种、可见干员最小绑定和跨编队头像档案。
- [x] C3：接入“继续”门禁、风险确认弹窗、二次复核和“用户确认完成”时间轴状态。
- [x] C4：同步领域文档、ADR、README 与实机验收清单。
- [x] C5：运行 workspace test、Clippy、Release build、格式与 diff 门禁，记录新二进制哈希。

### 当前发现与验证

- `ContinuationPlan` 已覆盖 test1 的 F1/F10/F11/F40/F240/F249，以及“在场干员后续再部署必须有
  头像档案”的用例；相关核心测试通过。
- Runner 继续门禁测试覆盖正非零暂停帧、运行态拒绝和末动作后拒绝；18 项 runner 测试通过。
- 全门禁通过：workspace tests（`repl-app` lib 31 项 + main 1 项、`repl-core` 113 项、
  `repl-input` 34 项及其余 crate/doctest）、Clippy `-D warnings`、Release build、格式与
  `git diff --check`。验收二进制 SHA-256 为
  `4D510B3146FA40C95437E3E2914E7E8E42A2C91C53CFA8A8E035EEFF267AF8BD`；实机仍待用户执行。

## 已有阶段：人工打轴作业编辑器（2026-07-29）

### 目标与成功标准

在现有 `repl-app` 内增加与复刻模式互斥的人工打轴编辑模式。编辑器必须能离线新建、打开、修改、
保存 MAA copilot 超集 JSON，并在尺子可用时用可信绝对帧驱动播放头和跟随光标；编辑模式不得创建
`Runner` / `Session`、调用 AFA 或产生任何输入。只有通过现有 `Copilot` 严格校验的文档才能切回
复刻模式运行。

完成标准：未知 JSON 字段往返保存不丢失；无效草稿可保存但不可运行；按干员分轨的时间轴支持
新增、删除、改帧、同帧重排和撤销/重做；Deploy 可通过逻辑地图或数值字段编辑位置与朝向；
复刻链路行为不变；自动测试、Clippy、Release 构建、格式和 diff 检查全部通过。

### 冻结边界

- 第一期只做人工创建 `Deploy` / `Skill` / `Retreat` / `Output`，不监听或识别真实鼠标操作。
- 编辑页集成在主程序内，但编辑会话是严格零输入会话；复刻运行期间禁止切换到编辑模式。
- `JobDocument` 是保留未知字段、允许无效草稿的编辑模型；`Copilot` 继续是严格、不可变的运行模型。
- 新动作使用时间轴中心光标帧；“跟随尺子”只把该光标吸附到最新可信绝对帧。
- 每个干员一条轨道，另有注释轨和未分配轨；同帧动作按显式序号依次执行。
- Deploy 使用逻辑关卡网格点选，MAA 资源不可用时保留 x/y 数值输入。
- 允许保存校验失败的 JSON 草稿；校验错误阻止运行，警告不阻止保存。

### 里程碑

- [x] E0：记录领域词汇与架构决定，复核并保护当前 AFA/M8 未提交改动。
- [x] E1：实现可往返保存的 `JobDocument`、诊断、编译边界及未知字段/草稿测试。
- [x] E2：实现编辑命令、稳定动作 ID、撤销/重做、同帧排序、脏标记和安全文件保存。
- [x] E3：实现 Slint 编辑页面、按干员分轨时间轴、属性面板和逻辑地图选择器。
- [x] E4：接入只读尺子跟随，完成复刻/编辑模式互斥及有效文档交回复刻模式。
- [x] E5：运行全工作区门禁并同步 README、`CONTEXT.md`、决策记录和 `docs/CURRENT_STATE.md`。

### 验证与风险

- 自动部署录制明确不在本阶段范围，不能用简化版鼠标监听替代人工编辑器的完整交付。
- `main.rs`、计划和状态文件在该阶段开始时曾有用户未提交改动；阶段收尾时已统一提交到 `develop`。
- Slint 使用软件渲染；时间轴只生成可见刻度和标记，不能为每一逻辑帧创建常驻组件。
- M8 已于 2026-07-29 由用户实机确认通过；编辑器自动门禁与 M8 相互独立。M9 已于 2026-07-30
  由用户确认 10/10，编辑器 GUI 仍待实机验收。

### 后续修复记录

- 2026-07-29：实机发现保存/另存为成功后 GUI 线程闪退。根因是保存回调把
  `RefCell::borrow_mut()` 临时值保持到整个 `match` 结束，成功分支刷新 UI 时发生重入借用 panic。
  已用回归测试稳定复现并改为在刷新前显式释放可变借用；全工作区门禁重新通过。

## 既有阶段：AFA 委托操作重构

## 目标与成功标准

把开局暂停、普通暂停/恢复、暂停技能和暂停撤退交给用户已启动的 AFA；复刻器只保留部署拖拽、
逐帧脉冲、鼠标定位/停靠以及 AFA 热键触发。每个真实输入动作都必须在派发后的新尺子
样本中确认仍停在目标绝对帧，最后一个动作确认后以零输入结束。

端到端成功的必要条件：AFA 可用性和权限预检通过；派发动作不会落入运行态；同帧动作
各自等待自己的新样本；最后动作完成后游戏画面和尺子都显示目标帧的 `1x_paused`；失败
时中止且不盲目重发输入。

## 冻结上下文

- 项目根目录：`D:\Arknights Operation Replicator\replicator`。
- 帧数唯一真源是 ArknightsCostBarRuler；AFA 是外部、由用户管理的进程。
- AFA 配置只读：`%APPDATA%\ArknightsFrameAssistant\PC\Settings.ini`。
- 只读核对同工作区的 AFA 源码后，`src/lib/config.ahk` 确认四个键名及默认值，发布工作流确认
  二进制名为 `AFA.exe`；这证明了接口命名，但不能替代用户机器上的实际 INI/热键验证。
- 当前代码已经完成主要委托链路和自动测试；外部程序始终由用户操作。用户实机日志已经证明
  链路可完成全流程，但也暴露开局尺子盲区和运行期 Pause 临界时序。
- 保护用户已有改动；本任务不修改 AFA 配置、不自动启动外部程序、不回退旧 Rust 技能/撤退时序。
- `examples/sample-job.json` 当前 10/40/60 只用于早帧 Resume/Pause/Pulse 回归；正式 M8 验收副本
  的首动作必须至少第 60 帧，不能把诊断样例的开局盲区失败算作正式稳定性结果。

## 已冻结的设计决定（2026-07-27）

### 1. 输入所有权

- AFA 负责：自动开局暂停、普通暂停、普通恢复、暂停技能、暂停撤退；逐帧所有权后来由 ADR-0010
  从 Rust 改为 AFA `33ms`。
- Rust 负责：部署拖拽、鼠标定位/停靠、发送一次 AFA 热键。
- 用户必须先进入关卡并等待 AFA 自动暂停，再点击“开始复刻”；开始预检只接受可信、战斗内、
  带 `frame_id` 的第 0 帧 `1x_paused` 尺子样本。非零帧必须走后续新增的风险确认式“继续”。
  `AutoBeginPause` 必须为 `1`；AFA 未自动暂停时开始请求直接被拒绝，不补发开局热键。
- 开始请求后 Runner 零输入等待用户手动把游戏切回前台，并要求焦点后的新 `frame_id` 样本；编队
  准备完成后再次等待最终焦点和新样本，再把控制权交给 Machine。等待期间不发送任何热键或脉冲。
- AFA 不可用、退出、降权、配置指纹变化或游戏失焦时，复刻器失败关闭；禁止回退旧 Rust
  技能/撤退三触控、Hover、固定长按等时序，也禁止静默重试。
- `Output` 和 `Finish` 都是零输入；旧 `after_last_action` 只兼容解析，不改变收尾行为。
- `initial_gap_ms` 后来由 ADR-0010 降为旧配置兼容字段，不参与主复刻。

### 2. ConfirmingAction 的确认窗口

确认屏障位于 `repl-app::Runner`，不修改 `repl-core::Machine` 的命令接口；Runner 已有尺子
引用和 `await_frame()`，由它阻塞等待确认样本。

每次动作派发前记录当前最新样本的 `min_frame_id`（至少取尺子最新值和 Machine 游标的较大
者）。确认只接受 `frame_id > min_frame_id` 的样本；同帧的下一动作必须重新记录水位，绝不
复用上一动作刚确认的那条样本。

样本判定固定如下。这里的 `is_running` 指尺子战斗状态归一化后的运行标志；不得把
`Snapshot::is_running`（费用条识别管线是否在轮询）误当成战斗运行态：

| 尺子样本 | 处理 |
| --- | --- |
| 任意 `is_running=true`（包括 `1x_running`、`0.2x_running` 等） | 立即失败；不等待、不补发 Pause。帧可能已经越过目标帧。 |
| 新的 `paused` 样本但为 `0.2x_paused` | 允许作为过渡样本等待；不能把它标记为动作完成。 |
| 新的、可信的 `1x_paused` 且 `elapsed == target_frame` | 确认动作完成。 |
| `paused` 但倍速为 `2x`、目标帧不一致、已离开战斗 | 失败；不得猜测或重试。 |
| `frame_id <= min_frame_id` | 旧样本，忽略。 |
| 暂停状态未知或尺子样本不可信 | 等待有界超时；超时即失败。 |

因此“短暂运行”不在 **AFA 委托动作的确认窗口** 内；窗口只容许暂停态，且最终必须回到
目标帧的可信 `1x_paused`。逐帧 `pulse()` 自己的脉冲收尾仍可保留 `1x_running → 1x_paused`
的正常过渡判定，两者不能混用。确认超时、尺子读取错误或取消都走统一失败路径。

### 3. AFA 适配器与 INI

- AFA controller 的配置构造路径（当前解析函数为 `AfaBindings::from_ini()`；如职责归并则为
  `AfaController::from_ini()`）内部集中定义四个协议键名：`PressPause`、`ReleasePause`、
  `PauseSkill`、`PauseRetreat`。源码已确认这些名称；运行时仍必须读取并记录用户 INI 中的
  实际值，不能把源码默认值当成用户配置，也不能静默回退。
- 首次实机启动/预检必须日志打印实际读取到的四个键值、配置路径、AFA 进程和权限；若键缺失、
  无法解析或与实机配置不符，预检失败并停止，不修改 INI。
- AFA 实机配置可能是带 BOM 的 UTF-16 LE；读取器按 BOM 接受 UTF-8、UTF-16 LE/BE，仍只读，
  不要求用户转换文件编码。
- `PressPause` 和 `ReleasePause` 是两个不同热键的两次独立 `key_tap`，不是同一按键的
  down/up；`AfaAction` 文档必须保持这一语义。
- 每次委托前重新检查 AFA 进程身份、管理员权限、配置指纹和游戏前台；前台检查失败直接
  中止，不调用 `SetForegroundWindow` 后偷偷重发热键。

### 4. 输入实现边界

- 键盘热键走 `SendInput` 键盘路径。
- `XButton1/2` 走 `MOUSEEVENTF_XDOWN/XUP`；滚轮走 `MOUSEEVENTF_WHEEL/HWHEEL`，实现位于
  `repl-input::mouse`，不能伪装成键盘事件。
- `PauseController::pulse()` 保留给逐帧脉冲；直接暂停入口已明确命名为
  `diagnostic_pause()`/`diagnostic_resume()`，仅供 `step-test` 等诊断工具使用，主运行路径不得
  调用它们执行普通暂停/恢复。

## 里程碑与进度

- [x] M0：读取项目规则、现有计划、状态、代码和 Git；冻结 AFA 委托边界及严格确认语义。
- [x] M1：补齐 XButton、上下/左右滚轮 `SendInput` 路径及局部单元测试。
- [x] M2：实现只读 AFA INI 解析、协议键名集中定义、进程/权限/指纹预检、热键分发和状态灯。
- [x] M3：Session 将普通暂停/恢复、Skill、Retreat 路由到 AFA；Deploy/Pulse 保持 Rust 直注入。
- [x] M3b：开局暂停改由 AFA `AutoBeginPause=1` 负责；Runner 只接受用户点击开始后的
  手动焦点交接和新尺子样本，不发送开局暂停键。
- [x] M4：Runner 实现每动作 `min_frame_id` 水位、运行态立即拒绝、`0.2x_paused` 过渡等待、
  目标帧 `1x_paused` 确认、同帧动作隔离及零输入 Finish；PauseController 直接暂停入口收紧为
  诊断命名，并用 `FakeFrameSource` 覆盖确认循环的新样本/运行态路径。
- [x] M5：补齐文档：ACCEPTANCE 删除旧 950ms/Hover/E5-4/E5-7 过期描述，加入 AFA 启动、
  配置核对、状态灯、前台失败和新确认语义；同步第三方声明、CURRENT_STATE 和本计划。
- [x] M6：补齐 AFA `AfaBindings`/INI 边界测试（缺键、重复键、键盘/XButton/滚轮、日志可观测性）。
- [x] M7：运行全工作区自动验证：
  `cargo test --workspace`、`cargo clippy --workspace --all-targets -- -D warnings`、
  `cargo build --release`，并检查 `cargo fmt --all`、`git diff --check`。
- [x] M8：用户使用 `docs/REAL_MACHINE_ACCEPTANCE.md` 指定哈希的本地构建，验证开始门禁、两次
  手动焦点交接、已有档案自动恢复、paused 首样本人工绑定、running 首样本无档案安全中止，以及
  Deploy/Skill/Retreat 全流程。每个运行区间只能派发一次 Resume/Pause，跨帧数必须为 0，最后动作
  后必须停在目标帧 `1x_paused` 且 Finish 零输入。外部程序只由用户操作。
- [x] M8A：新增独立 `afa-step-test` A/B 诊断工具；当时 `33ms` 仍为可选绑定。该决定已由
  ADR-0010 修订：主复刻现已使用同一热键，并将它列为预检必需项。
- [x] M9：2026-07-30 用户确认当前 AFA 逐帧构建完成录像和 CSV 连续 10 轮，动作生效帧 10/10
  与作业一致；录像、CSV 和日志未纳入仓库。

## 实机验收入口

1. 用户启动管理员权限的 AFA、ArknightsCostBarRuler 和《明日方舟》PC 客户端。
2. 首次预检核对当前 AFA 配置（本项目验收 profile 的预期值；不是 AFA 源码默认值）：
   `PressPause=g`、`ReleasePause=Space`、`PauseSkill=XButton2`、`PauseRetreat=XButton1`、`33ms=r`、
   `AutoBeginPause=1`、`DefaultStrongHoldProtocol=0`。若本 profile 的实际值不同，先在 AFA
   界面完成配置并重新预检；复刻器不修改 INI。若其他实际值不同，先记录实际
   值并停止本轮验收；只有协议键名/格式与源码不一致时才调整适配器集中常量/解析，不改 AFA 配置。
3. 按新启动流程先进入关卡并等待 AFA 暂停，点击开始后手动切回游戏；再验证普通暂停/恢复；
   分别以 Skill、Retreat 作为最后动作，确认画面和尺子都停在目标帧的 `1x_paused`，且 Finish
   没有补输入。
4. 通过后再跑 Deploy → Skill → Retreat 的同帧/跨帧组合和 §5.4 的录像核验。
5. 任一动作前台检查失败、AFA 失联、运行态样本出现或目标帧越过，立即记录日志并中止；
   不盲重试热键。

## 已验证证据

- `cargo test -p repl-input`：现役 35 个测试通过，包含 AFA UTF-16 LE/BE INI 和必需 `33ms` 回归覆盖。
- `cargo test -p repl-app`：历史阶段 25 个测试通过；新增目标 10、尺子最新 11 时禁止派发、目标帧最新可信
  样本允许派发，以及 Session 准备动作期间从 10 前进到 11 时由最终输入边界拒绝的回归覆盖。
- `cargo check -p repl-app`：通过。
- `cargo test --workspace`：该阶段通过（各 crate 全部测试和 doctest 通过；当时 `repl-input` 34 项，
  `repl-core` 100 项，`repl-app` 25 项）。现役总数见本计划顶部收尾记录。
- 2026-07-30 现役门禁：`repl-app` 库31项、主程序5项、`repl-core` 113项、`repl-input` 35项、
  `afa-step-test` 1项及其余工作区测试/doctest 全部通过；全工作区 Clippy、release 构建、格式和
  `git diff --check` 通过。标准路径二进制哈希见顶部阶段记录。
- `cargo clippy --workspace --all-targets -- -D warnings`：通过。
- `cargo build --release`：通过。
- `cargo fmt --all`、`git diff --check`：通过。
- 历史实机日志曾暴露动作前越帧、Pulse 旧 paused、Pause 样本计数和 Resume 重发问题；这些问题
  已分别由 ADR 0002–0005 的代码修复和回归测试覆盖。当前构建 M8 已于 2026-07-29 由用户按
  `docs/REAL_MACHINE_ACCEPTANCE.md` 确认通过；2026-07-30 用户进一步确认当前构建 M9 10/10 通过。

## 风险、发现与结果记录

- AFA 热键没有回执；尺子确认只能证明派发后停在目标帧，技能/撤退的画面效果仍需用户录像确认。
- 作业首动作没有第 60 帧下限；`examples/sample-job.json` 的 10/40/60 序列可用于精确链路验收。
  接管或运行期 Pause 越过下一动作时仍必须按尺子实际读数安全中止。
- 用户 AFA INI 实测为 UTF-16 LE；读取器已按 BOM 支持 UTF-8、UTF-16 LE/BE，始终只读。
- `Settings.ini` 的五个热键键名已由同工作区 AFA 源码确认，但实际值、热键捕获和动作效果仍必须由首次
  实机日志校验；发现协议格式不一致时先停用委托，不做兼容性猜测。
- 游戏失焦、AFA 重启、配置改动和非 1x 状态都视为不可恢复的本次运行失败。
- 动作前尺子权威、Pulse 收尾、Pause 宽限和 Resume running 回执的根因与决定分别记录在 ADR
  0002–0005；当前实现已有回归测试，M8 与 M9 均已由用户实机确认通过。
- 父目录旧二进制和早于当前验收构建的日志不能作为证据；当前验收对象及 SHA-256 以
  `docs/REAL_MACHINE_ACCEPTANCE.md` 为准。
- 每个里程碑完成后更新 `docs/CURRENT_STATE.md`；长期边界变化才新增 ADR，不把聊天记录当状态。
