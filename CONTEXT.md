# 帧复刻操作上下文

本上下文定义帧复刻器与 AFA 协作时使用的领域语言，避免把“输入已派发”误称为“动作已完成”。

## 帧与动作

**目标帧**：作业要求某个动作生效的绝对逻辑帧。它由尺子提供的累计帧数定义。

**动作派发**：复刻器或 AFA 收到一次输入请求的时刻；派发本身不等于游戏已经完成动作。

**动作前尺子屏障**：到达目标帧后的稳定等待不是盲等。Runner 在整个等待期持续读取尺子，只有
最新样本仍为目标帧的可信 `1x_paused` 才允许派发；Machine 游标不具有覆盖尺子读数的权力。

**动作确认**：尺子在动作派发之后提供新的、可信的暂停样本，并证明仍停在目标帧；只有确认后动作才算完成。

**暂停确认窗口**：动作派发后的样本窗口只容许暂停态。任何 `is_running=true` 的样本都表示帧
可能正在推进，必须立即判为失败；`0.2x_paused` 只能作为过渡等待，最终确认必须是目标帧的
可信 `1x_paused`。

**同帧动作隔离**：同一目标帧上的多个动作也必须逐个确认。每个动作都从自己的派发时刻开始
等待新的尺子样本，不能把上一动作已经确认过的样本当作下一动作的完成证据。

**直接输入原语**：由复刻器直接执行的部署拖拽、逐帧脉冲和鼠标定位。普通暂停、技能和撤退不属于直接输入原语。

**脉冲收尾证据**：逐帧 Pulse 是一个运行→暂停事务。尺子看到过运行态时，后续新暂停样本才是
最终落地；若运行态未被采到，必须连续两条新暂停样本才能证明稳定。单条暂停样本可能来自脉冲前
已进入分析管线的旧画面，不能单独结束事务。

**运行期暂停宽限**：AFA 普通暂停从热键派发到尺子确认之间允许游戏继续推进有限逻辑帧。宽限按
`elapsed - pause_sent_frame` 计算，不按 `frame_id` 样本数量计算；同一逻辑帧的重复分析结果不代表
游戏继续推进。

**运行期恢复回执**：AFA 普通恢复从热键派发到尺子首次报告可信 running 的事务边界。期间即使
收到 `frame_id` 更新的 paused/unknown 样本，也视为派发前已进入分析管线的积压画面：只推进样本
水位，不更新逻辑帧或暂停状态，不结束等待，更不能据此重发恢复热键。

## 外部协作

**AFA 委托动作**：开局暂停由 AFA 的自动开局暂停执行；普通暂停、普通恢复、暂停技能和暂停撤退
由已运行的 AFA 通过其现有热键执行。复刻器只读 AFA 配置；开局只等待确认，运行期动作只触发一次热键，不修改 AFA 的时序。

其中 `PressPause`、`ReleasePause` 是两个独立的热键动作，各自执行一次 `key_tap`；它们不是
同一个按键的按下与抬起。

**新样本水位**：每次动作派发前记录的最新 `frame_id`；确认阶段只能接受大于该水位的样本。

_避免_：把 `PressPause`/`ReleasePause` 叫作同一按键的按下和抬起；它们是两个独立热键动作。
