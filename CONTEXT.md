# 帧复刻操作上下文

本上下文定义帧复刻器与 AFA 协作时使用的领域语言，避免把“输入已派发”误称为“动作已完成”。

## 帧与动作

**目标帧**：作业要求某个动作生效的绝对逻辑帧。它由尺子提供的累计帧数定义。

**动作派发**：复刻器或 AFA 收到一次输入请求的时刻；派发本身不等于游戏已经完成动作。

**动作前尺子屏障**：到达目标帧后的稳定等待不是盲等。Runner 在整个等待期持续读取尺子，只有
最新样本仍为目标帧的可信 `1x_paused` 才允许派发；Machine 游标不具有覆盖尺子读数的权力。

**动作确认**：尺子在动作派发之后提供新的、可信的暂停样本，并证明仍停在目标帧；只有确认后动作才算完成。

**暂停确认窗口**：动作派发后的样本窗口只容许暂停态。任何 `is_running=true` 的样本都表示帧
可能正在推进，必须立即判为失败；`0.2x_paused` 只能作为过渡等待，最终确认必须是目标帧的
可信 `1x_paused`。

**同帧动作隔离**：同一目标帧上的多个动作也必须逐个确认。每个动作都从自己的派发时刻开始
等待新的尺子样本，不能把上一动作已经确认过的样本当作下一动作的完成证据。

**直接输入原语**：由复刻器直接执行的部署拖拽和鼠标定位。暂停、恢复、逐帧、技能和撤退均不属于直接输入原语。

**脉冲收尾证据**：AFA 逐帧动作是一个运行→暂停事务。尺子看到过运行态时，后续新暂停样本才是
最终落地；若运行态未被采到，必须连续两条新暂停样本才能证明稳定。单条暂停样本可能来自脉冲前
已进入分析管线的旧画面，不能单独结束事务。

**运行期暂停宽限**：AFA 普通暂停从热键派发到尺子确认之间允许游戏继续推进有限逻辑帧。宽限按
`elapsed - pause_sent_frame` 计算，不按 `frame_id` 样本数量计算；同一逻辑帧的重复分析结果不代表
游戏继续推进。

**运行期恢复回执**：AFA 普通恢复从热键派发到尺子首次报告可信 running 的事务边界。期间即使
收到 `frame_id` 更新的 paused/unknown 样本，也视为派发前已进入分析管线的积压画面：只推进样本
水位，不更新逻辑帧或暂停状态，不结束等待，更不能据此重发恢复热键。

## 外部协作

**AFA 委托动作**：开局暂停由 AFA 的自动开局暂停执行；普通暂停、普通恢复、`33ms` 逐帧、暂停技能和暂停撤退
由已运行的 AFA 通过其现有热键执行。复刻器只读 AFA 配置；用户在 AFA 已暂停后发起开始请求，
复刻器只等待焦点交接和新尺子样本，运行期动作只触发一次热键，不修改 AFA 的时序。逐帧默认绑定
为 R，但实际键值始终只读 `[Hotkeys]/33ms`，不得写死。

其中 `PressPause`、`ReleasePause` 是两个独立的热键动作，各自执行一次 `key_tap`；它们不是
同一个按键的按下与抬起。

**新样本水位**：每次动作派发前记录的最新 `frame_id`；确认阶段只能接受大于该水位的样本。

_避免_：把 `PressPause`/`ReleasePause` 叫作同一按键的按下和抬起；它们是两个独立热键动作。

## 启动接管

**开始请求**：用户在可信第 0 帧点击“开始复刻”的请求；它不会直接代表 Machine 已启动。

**继续截止帧**：用户在可信的非零暂停帧点击“继续”时冻结的绝对帧。该帧及以前的动作由用户
承担风险并声明已完成；后续样本推进不会自动扩大这个范围。

**用户确认完成**：继续时对截止帧及以前、但没有尺子动作确认记录的动作所作的风险声明。它与
“动作确认”不同：前者不证明输入派发或游戏画面结果，必须在时间轴和完成摘要中保持独立身份。

**推导场上状态**：按作业顺序回放截止帧及以前的 Deploy/Retreat 得到的干员位置集合。作业是
权威输入；继续功能不通过截图验证这个集合是否与真实画面一致。

**继续接管**：从冻结截止帧后的第一个动作创建全新 `Runner` / `Machine` 的启动过程，不是恢复
已经中止的旧线程。最终接管样本不得早于截止帧，也不得晚于下一动作帧。

**焦点交接**：复刻器等待用户手动把游戏窗口切回前台的过程；复刻器不自动调用游戏窗口的 focus。

**接管样本**：最终焦点交接后、且 frame_id 高于焦点前水位的第一条可信 1x 样本。它允许 paused 或 running，并建立 Machine 的绝对帧起点。

_Avoid_: 把“用户确认完成”称为“动作确认”，把继续称为“恢复旧会话”，把开始请求称为“开局等待
完成”，或把焦点交接称为“自动恢复游戏焦点”。

## 战前编队识别

**战前编队扫描**：用户在编队确认界面显式触发的只读截图与本地 OCR。它展示姓名、置信度、来源和
头像供人工确认，不发送 AFA、鼠标、触控或键盘输入，也不直接写入绑定档案。

**编队待用结果（Pending）**：用户确认后只在内存保留的一次性结果。它绑定当前作业编队指纹、游戏
窗口句柄、窗口尺寸和 30 分钟有效期，只能被下一次第 0 帧“开始复刻”消费；“继续”、换作业、取消、
重扫、窗口变化和过期都不能复用。

**F0 头像桥接**：第 0 帧先恢复已有 Session、全局头像和编队 profile，再用战前大头像解释仍未识别
的部署栏卡片。只有职业兼容、行列互为唯一最佳、达到 MAA 阈值且与次优保留安全间隔时才形成绑定；
失败或歧义必须回退人工绑定。成功桥接后保存的仍是实际 60×60 部署栏头像，不是战前大头像。

_Avoid_：把 OCR 建议称为自动绑定，把 Pending 称为持久化编队档案，或让战前扫描影响非零帧继续、
召唤物和地图装置的延迟绑定。

## 作业编辑

**编辑文档（`JobDocument`）**：面向人工打轴的宽松 JSON 文档。它保留未知字段，允许动作暂时
缺帧、缺干员或缺位置，也允许把这种无效草稿保存到磁盘。

**运行作业（`Copilot`）**：现有严格运行模型。编辑文档只有成功编译成运行作业，且没有错误级
诊断时，才可以交给复刻模式；警告不阻止运行。

**时间轴光标**：编辑时间轴中心所指的绝对逻辑帧。新动作创建在该帧；开启“跟随尺子”时，
光标只读跟随尺子的累计绝对帧，不会发送暂停、恢复或任何鼠标键盘输入。

**动作轨道**：Deploy、Skill、Retreat 按目标名称分轨；无名称但有格子的 Skill 进入地图装置轨；
Output 进入注释轨；既无名称也无格子的目标动作进入未分配轨。同一帧的动作仍按 JSON 数组中的
显式顺序执行，可在编辑器中上下调整。

**地图装置技能**：使用 MAA 兼容的 `Skill + location`。显式格子是权威目标，名称仅用于可读性和
轨道分组，可以留空；它不表示 Deploy，不触发部署栏扫描、延迟绑定或头像持久化。普通干员 Skill
省略 location 时，才按 Session 的既有部署记录解析名称。

**零输入编辑会话**：编辑模式只进行文档、文件和只读尺子操作。它不探测 AFA，不创建
`Session` / `Runner`，不定位游戏窗口，也不调用 `repl-input`。复刻运行期间禁止切回编辑模式。
